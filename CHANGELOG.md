# CHANGELOG

## ย้ายการ์ด SD ไปอยู่บัส SPI ของตัวเอง (Host 113.12.0 / Station 122.12.0)

ท่านถามว่าการแชร์บัสกับจอจะมีปัญหาไหม และแยกบัสได้หรือเปล่า

### คำตอบคือแชร์ได้จริง แต่แยกดีกว่าในงานนี้

การแชร์บัสเป็นวิธีมาตรฐาน และฝั่งซอฟต์แวร์ปลอดภัยแน่นอน — ผมไปอ่านซอร์สของ
ไลบรารี SD ใน arduino-esp32 แล้ว มันเรียก `beginTransaction()` / `endTransaction()`
ทุกครั้งเหมือนที่ Adafruit ทำ ทั้งสองตัวจึงผลัดกันใช้บัสอย่างถูกต้อง
และความเสี่ยงคลาสสิกเรื่องการ์ดไม่ยอมปล่อยขา MISO ก็ใช้ไม่ได้กับผังนี้
เพราะจอไม่มีสาย MISO ต่ออยู่เลย

แต่พอดูตัวเลขจริงแล้วพบสองอย่างที่เปลี่ยนคำตอบ

1. **จอวิ่งที่ 32 MHz** ค่า `SPI_DEFAULT_FREQ` ในไลบรารี ST77xx คือ 32,000,000
   การแตกแขนงสายเพิ่มไปหาการ์ดที่ความถี่นั้นทำให้สัญญาณแย่ลง
2. **แม่ข่ายใช้ SPI อยู่ตัวเดียวคือ FSPI ส่วน HSPI ว่างสนิท** ไม่ได้ใช้ทำอะไรเลย

### เหตุผลที่สำคัญที่สุดคือเรื่องการเดินสาย

การแชร์บัสแปลว่าต้อง **เอาสายสองเส้นยัดเข้าหัวต่อเดียวกัน** ที่ `GPIO13` กับ `GPIO14`
ซึ่งเป็นจุดที่หลุดง่ายที่สุดในงานที่ไขสายด้วยมือ

แบบแยกกลับ **เดินง่ายกว่า** คือสี่สายไปสี่ขาที่เรียงติดกันพอดี หนึ่งต่อหนึ่ง
และลำดับตรงกับลำดับขาบนตัวโมดูล เสียบตรงลงมาได้โดยไม่ต้องแยกสายเลย

| ขาบนหัวต่อการ์ด | ต่อไปที่ | ตำแหน่ง |
|---|---|:---:|
| `SD_SCK` | `GPIO6` | 6 |
| `SD_MISO` | `GPIO7` | 7 |
| `SD_MOSI` | `GPIO15` | 8 |
| `SD_CS` | `GPIO16` | 9 |

ได้ของแถมอีกสองอย่างคือ **สายหลุดเส้นเดียวไม่ดับทั้งคู่** และ **บัสของจอสะอาดเหมือนเดิม**
จ่ายแค่ขาสองขาที่ยังไงก็ไม่ได้ใช้อยู่แล้ว

เหตุผลที่ตำราส่วนใหญ่ให้แชร์บัสคือบอร์ดอย่าง Arduino Uno **มีขาไม่พอ**
ข้อจำกัดนั้นไม่มีอยู่ในบอร์ดนี้

### สิ่งที่แก้

* เพิ่ม `SPIClass SPI_SD(HSPI)` เป็นบัสของการ์ดโดยเฉพาะ
* **คืนบัสของจอกลับเป็นไม่มี MISO** เหมือนเดิม เพราะจอไม่เคยต้องอ่านข้อมูลกลับ
  รุ่นที่แล้วต้องใส่ MISO เข้าไปเพราะการ์ดใช้บัสร่วมกัน ตอนนี้ไม่ต้องแล้ว
* ตรวจแล้วว่า `SPIClass::begin()` คืนค่าทันทีถ้าบัสเปิดอยู่ การเรียกซ้ำตอนลองเมานต์ใหม่
  ทุกสามสิบวินาทีจึงปลอดภัย ไม่ได้เปิดบัสซ้ำซ้อน

### ไฟล์วงจรและกติกาตรวจสอบ

ไฟล์วงจร KiCad สร้างใหม่แล้ว การ์ดมีเน็ตของตัวเองครบสี่เส้น

และ **คืนกติกาของ `kicadcheck.py` ให้เข้มเหมือนเดิม** รุ่นที่แล้วต้องผ่อนให้
`TFT_SCL` กับ `TFT_SDA` มีสามปลายได้เพราะใช้บัสร่วมกัน ตอนนี้ไม่มีบัสที่ใช้ร่วมกันแล้ว
จึงตั้งรายการนั้นเป็นค่าว่าง กลับไปบังคับว่า **เน็ตสัญญาณทุกเส้นต้องมีสองปลายพอดี**
เก็บกลไกไว้เผื่ออนาคต แต่ตอนนี้ไม่มีข้อยกเว้นให้อะไรเลย

---

## บันทึกสำเนาลงการ์ด SD ที่อยู่หลังโมดูลจอ (Host 113.11.0 / Station 122.12.0)

ท่านส่งรูปโมดูลจอมาให้ดูว่ามีช่องเสียบการ์ด microSD อยู่ด้านหลัง และถามว่าเขียนโค้ดให้บันทึกได้เลยไหม

**ทำได้ครับ และมีสองอย่างที่ตรวจแล้วพบว่าเอื้อพอดี**

### หนึ่ง หัวต่อการ์ดเป็นคนละชุดกับหัวต่อจอ

จากรูป หัวต่อ `SD_SCK` `SD_MISO` `SD_MOSI` `SD_CS` อยู่ที่ขอบขวาของบอร์ด
แยกจากหัวแถว SPI ของจอที่อยู่ขอบซ้าย **ต้องเดินสายเพิ่มสี่เส้น** ไม่ได้ต่อมาให้แล้ว

แต่สองในสี่เส้นใช้บัสร่วมกับจอได้เลย เหลือแค่สองเส้นที่ต้องมีขาของตัวเอง
ซึ่ง **ลงตำแหน่ง 6 กับ 7 ที่ว่างอยู่พอดี** สายยังไม่ไขว้กันเหมือนผังเดิม

| ขาบนหัวต่อการ์ด | ต่อไปที่ | หมายเหตุ |
|---|---|---|
| `SD_SCK` | `GPIO14` | เส้นเดียวกับ TFT SCL |
| `SD_MISO` | `GPIO6` | ขาใหม่ ตำแหน่ง 6 |
| `SD_MOSI` | `GPIO13` | เส้นเดียวกับ TFT SDA |
| `SD_CS` | `GPIO7` | ขาใหม่ ตำแหน่ง 7 |

ใช้บัสร่วมกันได้เพราะแยกกันด้วยขา CS และ **จอในผังนี้ไม่ได้ใช้ขา MISO** จึงไม่แย่งสายกัน
ของเดิมเปิดบัสด้วย `SPI_TFT.begin(TFT_SCLK, -1, TFT_MOSI, TFT_CS)` คือไม่มี MISO
ต้องแก้ให้มี ไม่งั้นอ่านการ์ดไม่ได้

### สอง ลำดับในโค้ดเดิมเอื้อพอดี

`processScanRequest()` **ส่งคำตอบให้จุดบริการก่อน แล้วค่อยเขียนไฟล์**
การ์ด SD เขียนช้ากว่าแฟลชในบอร์ดมาก บางจังหวะการ์ดหยุดลบบล็อกภายในเป็นร้อยมิลลิวินาที
ถ้าลำดับกลับกัน จุดบริการอาจรอจนหมดเวลาแล้วขึ้นว่าไม่ทราบผท ทั้งที่ทุกอย่างปกติ
ตรงนี้โค้ดเดิมทำถูกอยู่แล้ว ไม่ต้องแก้

### การ์ดเป็นสำเนา ไม่ใช่ที่เก็บหลัก

**LittleFS ยังเป็นที่เก็บหลักเหมือนเดิมทุกประการ** ตั้งใจออกแบบแบบนี้
เพราะการ์ดถอดออกได้ เสียได้ และไม่มีก็ได้ **ถ้าให้การ์ดเป็นที่เก็บหลัก
วันที่ลืมเสียบการ์ดคือวันที่ระบบจ่ายอาหารไม่ได้** ซึ่งขัดกับหลักที่วางกันไว้
ว่าระบบต้องไม่หยุดเพราะอุปกรณ์เสริม

| ไฟล์บนการ์ด | เขียนเมื่อไร |
|---|---|
| `/canteen/daily_log.csv` | ทุกครั้งที่มีนิสิตรับสิทธิ์สำเร็จ |
| `/canteen/arc_YYYYMMDD_HHMMSS.csv` | ตอนปิดยอดประจำวัน |
| `/canteen/students_backup.csv` | ทุกครั้งที่ทะเบียนนิสิตเปลี่ยน |

ประโยชน์จริงคือ **ถ้าบอร์ดแม่ข่ายพัง ถอดการ์ดไปเสียบคอมก็ได้ข้อมูลครบ**
ไม่ต้องไปกู้จากแฟลชในชิป และเก็บประวัติย้อนหลังได้ไม่ติดเพดาน 1 MB ของแฟลช

ถ้าไม่มีการ์ดก็ข้ามการเขียนไปเฉย ๆ และลองเมานต์ใหม่ทุกสามสิบวินาที
**เสียบการ์ดทีหลังได้โดยไม่ต้องรีบูต** และถ้าการ์ดหลุดกลางวันแล้วเสียบคืน
ระบบกลับมาบันทึกต่อเองโดยไม่ต้องมีใครสั่ง

เพิ่มแผงสถานะในหน้าตั้งค่าบนเว็บ บอกว่ากำลังบันทึกอยู่หรือไม่ ความจุเท่าไร
และเขียนพลาดไปกี่ครั้ง

### ตัวตรวจที่ผ่านทั้งที่ไม่ได้ตรวจ

หลังเพิ่มขา `SD_MISO_PIN` กับ `SD_CS_PIN` แล้ว `pincheck.py` ยังขึ้นว่าผ่าน
ดูเผิน ๆ เหมือนดี แต่ผมไปดูจำนวนขาที่มันรายงานแล้วพบว่า **ยังนับได้ 12 ขาเท่าเดิม**

ต้นเหตุคือ regex ของมันจับเฉพาะชื่อที่ขึ้นต้นด้วย `TFT_` `RC522_` `BUZZER` และอื่น ๆ
ที่มีอยู่ตอนเขียน **ขาที่ขึ้นต้นด้วย `SD_` จึงถูกมองข้ามเงียบ ๆ**
เป็นบั๊กชนิดเดียวกับที่เจอใน `packetcheck.py` รอบก่อน คือตัวตรวจที่บอกว่าผ่าน
ทั้งที่ไม่ได้ตรวจอะไรเลย แก้ regex แล้วตอนนี้นับได้ 14 ขาถูกต้อง

### ไฟล์วงจรตามไปด้วย

เพิ่มสัญลักษณ์หัวต่อการ์ดลงในวงจรของแม่ข่าย สร้างไฟล์ใหม่ด้วยสคริปต์
แล้วให้ KiCad เอ็กซ์พอร์ต netlist ออกมาตรวจ

ต้องแก้กติกาของ `kicadcheck.py` ด้วย เพราะเดิมบังคับว่า **เน็ตสัญญาณต้องมีสองปลายพอดี**
แต่ตอนนี้ `TFT_SCL` กับ `TFT_SDA` มีสามปลายโดยตั้งใจ คือบอร์ด จอ และการ์ด
เปลี่ยนเป็น "ต้องมีอย่างน้อยสองปลาย และถ้าเกินสองต้องอยู่ในรายการบัสที่ใช้ร่วมกัน"
ซึ่งเข้มกว่าเดิมด้วยซ้ำ เพราะยังจับกรณีขาชนกันโดยไม่ตั้งใจได้อยู่
พิสูจน์แล้วโดยลองเอา `TFT_SCL` ออกจากรายการ ตัวตรวจฟ้องทันที

### ก่อนใช้งาน

* การ์ดต้องฟอร์แมตเป็น **FAT32** ความจุไม่เกิน 32 GB
* เดินสายสี่เส้นตามตารางข้างบนก่อนแฟลช
* ถ้าเสียบการ์ดแล้วหน้าตั้งค่ายังขึ้นว่าไม่พบ ให้ดูสาย `SD_MISO` ก่อน
  เพราะเป็นเส้นเดียวที่ระบบเดิมไม่เคยใช้

---

## ไฟล์วงจรสำหรับ KiCad ทั้งสองบอร์ด (Host 113.10.0 / Station 122.12.0)

ท่านขอแบบการต่อวงจรเพื่อเอาไปใช้ใน KiCad

### สร้างด้วยสคริปต์ ไม่ได้วาดมือ

ผังขาอยู่ในซอร์สของเฟิร์มแวร์ ถ้าวาดมือแล้ววันหนึ่งย้ายขา
**ไฟล์วงจรจะเพี้ยนจากของจริงโดยไม่มีอะไรฟ้องเลย** ช่างก็จะเดินสายตามแบบที่ผิด
จนกว่าจะประกอบเสร็จแล้วใช้ไม่ได้ ซึ่งเป็นความผิดพลาดที่แพงที่สุดในงานฮาร์ดแวร์

`hardware/kicad/generate_kicad.py` จึง **อ่านค่า `#define` ของขาจากซอร์สจริง**
แล้วสร้างไฟล์วงจรใหม่ทั้งหมด สั่งรันซ้ำเมื่อไรก็ตรงกับเฟิร์มแวร์เสมอ

### สิ่งที่ได้

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `Canteen_Host.kicad_sch` · `.kicad_pro` | วงจรเครื่องแม่ข่าย เปิดด้วย KiCad ได้เลย |
| `Canteen_Station.kicad_sch` · `.kicad_pro` | วงจรจุดบริการ |
| `MCUPR_Canteen.kicad_sym` | ไลบรารีสัญลักษณ์ |
| `*.net` | netlist สำหรับนำเข้า Pcbnew และใช้ตรวจสอบ |
| `*.pdf` | ภาพวงจรสำหรับพิมพ์ ไม่ต้องมี KiCad ก็เปิดดูได้ |

**สัญลักษณ์ทุกตัวฝังอยู่ในไฟล์วงจรแล้ว** เปิดที่เครื่องไหนก็ได้โดยไม่ต้องติดตั้งไลบรารีเพิ่ม
ซึ่งจำเป็น เพราะบอร์ด ESP32-S3 DevKitC-1 จอ ST7789 และ RC522
ไม่มีอยู่ในไลบรารีมาตรฐานของ KiCad

### อ่านวงจรนี้อย่างไร

ไม่ได้ลากเส้นยาวข้ามหน้า แต่ใช้ **ป้ายชื่อเน็ต** ขาไหนที่มีป้ายชื่อเดียวกันคือขาที่ต่อถึงกัน
วิธีนี้ตรงกับงานเดินสายจริงมากกว่า เพราะสิ่งที่ช่างต้องรู้คือ "ขานี้ไปขาไหน"
ไม่ใช่ "เส้นวิ่งอ้อมทางไหน" และหน้ากระดาษก็ไม่รก

สัญลักษณ์ `U1` นับขา 1 ถึง 22 เป็นแถวซ้ายจากบนลงล่าง และ 23 ถึง 44 เป็นแถวขวา
ตรงกับตำแหน่งจริงบนบอร์ดและตรงกับตารางใน `docs/HARDWARE.md`

วงจรมีมากกว่าผังขา คือมีวงจรแบ่งแรงดัน 1:2 สำหรับวัดแบตเตอรี่
ตัวเก็บประจุกรองไฟของแต่ละโมดูล และตัวกรองสัญญาณรบกวนที่จุดวัด

### ตรวจด้วย KiCad ตัวจริง ไม่ได้เดาเอา

ผมติดตั้ง **KiCad 7 ลงในเครื่องที่ทำงานอยู่** เพื่อให้ตัวโปรแกรมเป็นคนบอกว่าไฟล์ถูกต้อง
ไม่ใช่ผมเดาเองแบบที่เคยพลาดมาแล้วในเซสชันนี้

* `kicad-cli sch export netlist` อ่านไฟล์วงจรได้และคำนวณเน็ตออกมาครบ
* `kicad-cli sch export pdf` และ `svg` พล็อตออกมาได้ ผมเปิดดูภาพแล้วจัดหน้าใหม่สองรอบ
  เพราะรอบแรกข้อความอ้างอิงทับตัวสัญลักษณ์ และภาษาไทยในกรอบชื่อเรื่องขึ้นเป็นกล่องสี่เหลี่ยม
  เนื่องจากฟอนต์มาตรฐานของ KiCad ไม่มีตัวอักษรไทย จึงเปลี่ยนกรอบชื่อเรื่องเป็นภาษาอังกฤษ

### ตัวตรวจใหม่ kicadcheck.py

เป็นตัวที่สิบ และทำงานต่างจากตัวอื่น คือ **ไม่ได้อ่านไฟล์วงจรตรง ๆ**
แต่อ่าน **netlist ที่ KiCad คำนวณออกมาเอง** แล้วเทียบกับค่าขาในซอร์สเฟิร์มแวร์
เท่ากับให้ KiCad เป็นคนยืนยันว่าเน็ตเชื่อมถึงกันจริง แล้วเราค่อยเทียบว่าตรงกับโค้ดไหม

ตรวจสามข้อ

1. ทุกขาที่เฟิร์มแวร์ใช้ ต้องอยู่ในเน็ตชื่อเดียวกันในไฟล์วงจร
2. เน็ตสัญญาณต้องมีสองปลายพอดี ปลายเดียวคือลืมต่อ สามปลายคือชนกัน
3. ขา GPIO หนึ่งขาต้องไม่ไปโผล่อยู่สองเน็ต

พิสูจน์แล้วโดยลองย้ายบัซเซอร์ในโค้ดจาก GPIO17 ไป GPIO16 โดยไม่สร้างแบบใหม่
ตัวตรวจฟ้องทันทีว่า `U1 ขา 9 ควรอยู่เน็ต BUZZER แต่ไม่อยู่`

---

## บอกสถานะก่อนนิสิตแตะบัตร และแยกกรณี "ไม่ทราบผล" ออกมา (Host 113.10.0 / Station 122.12.0)

ตามที่ตกลงกันไว้ว่าจะทำสองอย่าง

### หนึ่ง หน้าแรกบอกสถานะตั้งแต่ก่อนแตะบัตร

ของเดิมนิสิตต้องแตะก่อน แล้วยืนรอสามวินาที ถึงจะรู้ว่าแม่ข่ายล่ม
หลักสากลของเครื่องบริการตัวเองคือ **ต้องขึ้นสถานะหยุดให้บริการตั้งแต่ก่อนมีคนมาใช้**

หน้าแรกจึงบอกสถานะการให้บริการสามแบบ

| สถานะ | พาดหัว | สีคลื่นของสัญลักษณ์ |
|---|---|---|
| พร้อมรับบัตร | `TAP YOUR CARD` | เขียว |
| แม่ข่ายไม่ตอบ | `SERVER OFFLINE` | **เหลือง** |
| เครื่องอ่านบัตรไม่ตอบ | `OUT OF SERVICE` | **แดง** |

แยกสองความผิดพลาดด้วยคนละสี เพราะเป็นคนละเรื่องและแก้คนละทาง
เหลืองคือรอระบบกลับมา แดงคือฮาร์ดแวร์ของเครื่องนี้เสียต้องตามช่าง

และ **ปฏิเสธการแตะบัตรตั้งแต่ต้น** เมื่อรู้อยู่แล้วว่าแม่ข่ายไม่ตอบ
ไม่ส่งอะไรออกไปเลย รู้ผลทันทีไม่ต้องรอหมดเวลา

### สอง แยก "ไม่สำเร็จ" ออกจาก "ไม่ทราบผล"

นี่คือแกนของเรื่องทั้งหมด เดิมสองกรณีนี้ใช้หน้าจอเดียวกัน ทั้งที่ต่างกันโดยสิ้นเชิง

| กรณี | ตัดสิทธิ์ไปหรือยัง | หน้าจอใหม่ |
|---|---|---|
| รู้อยู่แล้วว่าแม่ข่ายไม่ตอบ จึงไม่ได้ส่งอะไรเลย | **ไม่** การันตีได้ | `SERVER OFFLINE` + *Nothing was taken from your quota* |
| ส่งไปแล้วไม่มีคำตอบใน 3 วินาที | **ไม่ทราบ** | `RESULT UNKNOWN` + *Your meal may or may not be recorded* |

กรณีที่สองเกิดเมื่อแม่ข่ายอนุมัติและบันทึกไปแล้ว แต่แพ็กเก็ตตอบกลับหายทั้งสามครั้ง
**การขึ้นว่า "ไม่สำเร็จ" ในกรณีนี้คือการบอกข้อมูลผิด** เพราะมันอาจสำเร็จไปแล้วจริง ๆ

เรื่องนี้แก้ให้หายขาดไม่ได้ในทางทฤษฎี เป็นปัญหา **Two Generals**
มาตรฐานจึงไม่พยายามกำจัด แต่ทำให้ **กู้คืนได้** แทน
แถบล่างของหน้านั้นจึงบอกว่า *Tap your card again to see the real result*
ซึ่งใช้กลไกที่ระบบมีอยู่แล้ว ถ้าบันทึกไปแล้วจะได้ `ALREADY SERVED`
ถ้ายังไม่ได้บันทึกก็จะตัดสิทธิ์ให้ตามปกติ **ไม่ว่าทางไหนก็ได้คำตอบที่ถูกต้อง และไม่มีทางจ่ายซ้ำ**

### สิ่งที่ตรวจแล้วพบว่าออกแบบไว้ดีอยู่แล้ว

ระหว่างไล่โค้ดเพื่อตอบคำถามนี้ พบว่าแม่ข่าย **กันการทำซ้ำด้วยคู่ `(หมายเลขจุดบริการ, seq)`
แล้วส่งคำตอบเดิมที่แคชไว้กลับไป ไม่ประมวลผลใหม่** ซึ่งคือ idempotent request ตามมาตรฐาน
ถ้าไม่มีอันนี้ การส่งซ้ำสามครั้งของจุดบริการจะกลายเป็นตัดสิทธิ์สามครั้ง
ส่วนนี้ไม่ต้องแก้อะไรเลย

### ตรวจแล้ว

เพิ่มกรณีทดสอบระดับพิกเซลสามกรณีสำหรับการสลับสถานะ
พร้อม→แม่ข่ายล่ม, แม่ข่ายล่ม→พร้อม และเครื่องอ่านเสีย→แม่ข่ายล่ม
**ทุกกรณีตรงกับการวาดใหม่ทั้งหน้าทุกพิกเซล ทั้งโหมดมืดและสว่าง**
เส้นทางที่วาดทุกวินาทียังล้างพื้น 0 ครั้ง ตัวตรวจทั้งเก้าตัวผ่าน
และเพิ่มหัวข้อ **เมื่อเครื่องแม่ข่ายไม่ตอบ** ลงใน README อธิบายนโยบายทั้งหมด

---

## จัดขาใหม่ทั้งสองบอร์ดให้สายไม่ไขว้กัน (Host 113.10.0 / Station 122.11.0)

ท่านขอให้ออกแบบการเชื่อมต่อใหม่ทั้งสองฝั่ง **ให้ขาของอุปกรณ์เรียงติดกัน ไม่ข้ามกัน**
เพื่อให้เดินสายง่าย พร้อมส่งผังขาของบอร์ดมาให้ดู

### ปัญหาของผังเดิม

ฝั่งจุดบริการหนักที่สุด ขาของ RC522 กระจายอยู่สี่ที่

| ขา | ผังเดิม | อยู่ตรงไหน |
|---|---|---|
| SCK | GPIO4 | แถวซ้าย ตำแหน่ง 4 |
| MOSI | GPIO5 | แถวซ้าย ตำแหน่ง 5 |
| SS | GPIO7 | แถวซ้าย ตำแหน่ง 7 |
| RST | GPIO17 | แถวซ้าย ตำแหน่ง 10 |
| **MISO** | **GPIO21** | **แถวขวา ตำแหน่ง 18** |

สาย MISO เส้นเดียวต้องอ้อมข้ามบอร์ดไปอีกฝั่ง และไฟหน้าจอ (BLK) ก็อยู่ที่ GPIO15
ซึ่งห่างจากขาจออีกห้าขาที่เหลือ

### ผังใหม่

**อุปกรณ์ภายนอกทุกตัวอยู่บนแถวซ้ายของบอร์ดแถวเดียว แถวขวาไม่ได้ต่ออะไรเลย**

| ตำแหน่ง | ขา | เครื่องแม่ข่าย | จุดบริการ |
|:---:|:---|:---|:---|
| 4 | GPIO4 | DS3231 SDA | RC522 SDA (SS) |
| 5 | GPIO5 | DS3231 SCL | RC522 SCK |
| 6 | GPIO6 | ว่าง | RC522 MOSI |
| 7 | GPIO7 | ว่าง | RC522 MISO |
| 8 | GPIO15 | ว่าง | RC522 RST |
| 10 | GPIO17 | บัซเซอร์ | บัซเซอร์ |
| 11 | GPIO18 | ปุ่มกด | ปุ่มกด |
| 12 | GPIO8 | วัดแบตเตอรี่ | วัดแบตเตอรี่ |
| 15–20 | GPIO9–14 | จอ BLK CS DC RES SDA SCL | จอ BLK CS DC RES SDA SCL |

**บัซเซอร์ ปุ่มกด และสายวัดแบตเตอรี่ใช้ขาเดียวกันทั้งสองบอร์ด**
ประกอบสายชุดเดียวแล้วสลับใช้ได้ทั้งสองฝั่ง

### จอเสียบตรงลงมาได้ทั้งแถว

จุดที่ลงตัวที่สุดของผังนี้ โมดูลจอ 8 ขาเรียงว่า `GND VCC SCL SDA RES DC CS BLK`
พอไล่ตำแหน่งบนบอร์ด **จากล่างขึ้นบน** จะได้

```
ตำแหน่ง 22 GND  <- GND      ตำแหน่ง 18 GPIO12 <- RES
ตำแหน่ง 21 5V0  <- VCC      ตำแหน่ง 17 GPIO11 <- DC
ตำแหน่ง 20 GPIO14 <- SCL    ตำแหน่ง 16 GPIO10 <- CS
ตำแหน่ง 19 GPIO13 <- SDA    ตำแหน่ง 15 GPIO9  <- BLK
```

**ตรงกันขาต่อขาทั้งแปดเส้น** สายแพเส้นเดียววางขนานลงมาได้เลย
ที่ลงตัวเพราะขา CS DC RES SDA SCL ของจอ **ใช้ GPIO10 ถึง 14 อยู่แล้วตั้งแต่ต้น**
ย้ายแค่ BLK จาก GPIO15 มาที่ GPIO9 เท่านั้น จอจึงแทบไม่ต้องรื้อสายเลย

> ⚠️ จอ ST7789 บางรุ่นรับ 3.3V เท่านั้น ถ้าเป็นแบบนั้นให้ต่อ VCC ไปที่ขา 3V3 ด้านบน
> แทนตำแหน่ง 21 **ตรวจสเปกจอของท่านก่อนจ่ายไฟ**

### เจอปัญหาเดิมที่ซ่อนอยู่ระหว่างทาง

ระหว่างเขียนตัวตรวจ พบว่าผังเดิมของฝั่งจุดบริการ
**วัดแรงดันแบตเตอรี่ที่ GPIO3 ซึ่งเป็นขา strapping** (ใช้เลือกแหล่ง JTAG ตอนบูต)
ต่อวงจรแบ่งแรงดันค้างไว้ที่ขานี้ทำให้ระดับแรงดันตอนบูตเพี้ยนได้
ผังใหม่ย้ายไปที่ GPIO8 ซึ่งเป็น ADC1_CH7 ปกติ

### ตัวตรวจใหม่ pincheck.py

เพิ่มเป็นตัวที่เก้า อ่านค่าขาจากซอร์สจริงของทั้งสองบอร์ด แล้วฟ้องเมื่อ

* มีขาไหนถูกใช้ซ้ำสองหน้าที่ในบอร์ดเดียวกัน
* มีขาไหนไปโดนขาต้องห้าม โดยเฉพาะ **GPIO33–37 ที่ PSRAM แบบ OPI จองไว้**
  ซึ่ง GPIO35 36 37 โผล่อยู่บนแถวขวาของบอร์ดและดูเหมือนใช้ได้ เป็นกับดักที่เจอบ่อยที่สุด
* ขาวัดแบตเตอรี่ไม่ได้อยู่ใน ADC1 เพราะ **ADC2 อ่านไม่ได้ขณะเปิด Wi-Fi**

พิสูจน์แล้วสองทาง รันกับผังเดิมจับ GPIO3 ได้ และลองย้ายบัซเซอร์ไปทับขาจอ
ก็ฟ้องว่า GPIO14 ถูกใช้ซ้ำ

### เอกสารใหม่

ผังเต็มพร้อมลำดับขาของทุกโมดูล ข้อควรระวังเรื่องแรงดัน และรายการขาต้องห้าม
อยู่ใน [`docs/HARDWARE.md`](docs/HARDWARE.md)

**ครั้งนี้ต้องรื้อสายจริงนะครับ** โดยเฉพาะฝั่งจุดบริการ
ตรวจให้ครบก่อนจ่ายไฟ และถ้าจอไม่ติดหลังแฟลช ให้ดูขา BLK ที่ย้ายจาก GPIO15 ไป GPIO9 ก่อน

---

## ต้นเหตุจริงของการหน่วงห้าวินาที: วิทยุของจุดบริการหลับ (Host 113.9.0 / Station 122.10.0)

ท่านแจ้งว่าหลังแก้รอบที่แล้ว **ยังหน่วงราวห้าวินาทีเท่าเดิม**

แปลว่าคำวินิจฉัยเดิมของผมยังไม่ถึงต้นตอ รอบที่แล้วผมแก้ **ฝั่งผู้ส่ง**
ให้ส่งยูนิแคสต์แทนการกระจายเสียงอย่างเดียว แต่ปัญหาอยู่ที่ **ฝั่งผู้รับ**

### ไล่ใหม่จากหลักฐาน

ลูปของจุดบริการตรวจ `pendingConfigUpdate` ทุกหนึ่งรอบ ซึ่งวนเร็วราวสองมิลลิวินาที
ถ้าแพ็กเก็ตมาถึงจริง คำสั่งจะถูกทำภายในเสี้ยววินาที **แปลว่าแพ็กเก็ตไม่ได้มาถึง**

แล้วทำไมมันมาถึงตอนห้าวินาที คำตอบคือ **มันมาถึงตอนที่จุดบริการเพิ่งส่ง heartbeat ออกไปเอง**
ทิศทางจากจุดบริการไปแม่ข่ายเร็วเสมอ แต่ทิศกลับช้า นั่นคือลายเซ็นของ
**ตัวรับที่หลับอยู่ แล้วตื่นเฉพาะตอนที่ตัวเองเพิ่งส่ง**

### ต้นเหตุ

โค้ดฝั่งจุดบริการสั่งปิดโหมดประหยัดพลังงานไว้แล้วด้วย `esp_wifi_set_ps(WIFI_PS_NONE)`
ดูเผิน ๆ เหมือนถูกต้อง แต่ **Arduino core สั่งทับกลับ**

ผมดาวน์โหลดซอร์สของ core มาอ่านยืนยัน ในไฟล์ `libraries/WiFi/src/STA.cpp`

```c
if (ev->event_id == ARDUINO_EVENT_WIFI_STA_START) {
  _sta_network_if->_setStatus(WL_DISCONNECTED);
  if (esp_wifi_set_ps(WiFi.getSleep()) != ESP_OK) {   // <-- ทับค่าที่เราตั้ง
```

`WiFi.getSleep()` คืนค่าที่ core จำไว้ ซึ่งใน `WiFiGeneric.cpp` ตั้งค่าเริ่มต้นไว้ว่า

```c
#if CONFIG_IDF_TARGET_ESP32S2
wifi_ps_type_t WiFiGenericClass::_sleepEnabled = WIFI_PS_NONE;
#else
wifi_ps_type_t WiFiGenericClass::_sleepEnabled = WIFI_PS_MIN_MODEM;   // <-- ESP32-S3 มาทางนี้
#endif
```

อีเวนต์ `STA_START` เป็นแบบ **อะซิงโครนัส** มันถูกส่งมาจากทาสก์อีเวนต์ของ core
จึงมาถึง **หลัง** บรรทัด `esp_wifi_set_ps(WIFI_PS_NONE)` ของเราไปแล้ว
ค่าที่เราตั้งจึงถูกเปลี่ยนกลับเป็นโหมดประหยัดพลังงานเงียบ ๆ ไม่มีคำเตือนอะไรทั้งสิ้น

ผลคือวิทยุของจุดบริการหลับเป็นช่วง ๆ แพ็กเก็ตที่แม่ข่ายส่งมาแบบไม่ได้นัดหมายจึงตกหล่น
จะได้รับแน่ ๆ ก็ตอนที่เพิ่งส่ง heartbeat ออกไปเอง ซึ่งเกิดทุก
`6000 + หมายเลขสถานี × 350` มิลลิวินาที **ตรงกับห้าวินาทีที่ท่านจับได้พอดี**

### วิธีแก้

ใช้ `WiFi.setSleep(false)` แทน ซึ่งเปลี่ยน **ทั้งค่าที่ core จำไว้และค่าที่ใช้งานจริง**
อีเวนต์ที่ตามมาภายหลังจึงสั่งค่าเดิมซ้ำ ไม่ได้ทับให้กลับไปหลับอีก
ส่วน `esp_wifi_set_ps(WIFI_PS_NONE)` เดิมยังคงไว้เป็นชั้นสำรอง

### ทำไมฝั่งแม่ข่ายไม่เป็น

แม่ข่ายเป็น AP และไฟล์ `AP.cpp` ของ core **ไม่มี** การสั่งทับแบบนี้เลย
ค่าที่แม่ข่ายตั้งไว้จึงอยู่ครบ ตรงกับที่สังเกตได้ว่าทิศจากจุดบริการมาแม่ข่ายเร็วเสมอ

### เสริมความทนทานฝั่งแม่ข่ายไปด้วย

ของเดิมย้ำคำสั่งสามรอบติดกันด้วย `delay(30)` รวมแล้วกินเวลาแค่ 90 มิลลิวินาที
ถ้าจุดบริการพลาดช่วงสั้น ๆ นั้นไป ก็ต้องรอ heartbeat รอบถัดไปอยู่ดี

เปลี่ยนเป็นย้ำหกรอบ **กระจายออกไปราวสองวินาที โดยให้ `loop()` เป็นคนส่ง**
ไม่หน่วงลูปเลยสักมิลลิวินาที หน้าเว็บจึงตอบกลับทันทีและจอของแม่ข่ายไม่สะดุด
ข้อดีคือทนทานต่อการตกหล่นของสัญญาณโดยไม่ต้องพึ่งว่าคำวินิจฉัยข้างบนถูกต้อง 100%

### ขออภัยที่ต้องแก้สองรอบ

รอบที่แล้วผมด่วนสรุปจากบั๊กที่เห็นชัดในโค้ด คือแพ็กเก็ตกระจายเสียงที่ใส่ฟิลด์ไม่ครบ
ซึ่ง **เป็นบั๊กจริงและควรแก้** แต่ไม่ใช่ตัวที่ทำให้หน่วง ผมน่าจะเอะใจตั้งแต่ตอนที่ท่าน
บอกว่าการสลับสว่างมืดก็ช้าเท่ากัน ทั้งที่ค่าโหมดสีนั้นส่งถูกต้องมาตลอด
จุดนั้นคือหลักฐานว่าปัญหาอยู่ที่ตัวรับ ไม่ใช่ตัวส่ง

---

## ขีดสัญญาณมุมขวาบนของจุดบริการหายไป (Host 113.8.0 / Station 122.9.0)

ท่านแจ้งว่าฝั่งจุดบริการ **ไม่แสดงสัญลักษณ์ระดับสัญญาณ แต่แสดงเป็นตัวเลขแทน**
และต้องการให้เหลือแค่สัญลักษณ์ ไม่ต้องการตัวเลข

เป็นสองเรื่องที่มาพร้อมกัน เรื่องหนึ่งเป็นบั๊ก อีกเรื่องเป็นสิ่งที่ท่านอยากได้

### บั๊ก ขีดสัญญาณหายถาวรหลังเปลี่ยนหน้าครั้งแรก

`drawStationTopBar()` ซึ่งถูกเรียกทุกครั้งที่วาดหน้าใหม่ เริ่มด้วยการล้างแถบบนทั้งแถบ
`tft.fillRect(0, 0, 320, 24, ...)` แล้ววาดคืนแค่ **ชื่อหน้ากับแบตเตอรี่**
ขีดสัญญาณที่เพิ่งถูกลบไปไม่มีใครวาดคืนให้

แล้วทำไมมันไม่กลับมาเองในรอบถัดไป เพราะ `updateTopRightHeaderSmooth()` ที่เดินทุกวินาที
**จำไว้ว่าเคยวาดกี่ขีด** ถ้าจำนวนขีดกับสถานะออนไลน์ยังเท่าเดิม มันจะ `return` ทิ้งทันที
โดยไม่วาดอะไรเลย ซึ่งเป็นกลไกกันจอกะพริบที่ใส่ไว้ตั้งแต่รุ่น 122.3.0

สองอย่างนี้รวมกันแล้วได้ผลลัพธ์คือ **ขีดหายไปเลยจนกว่าความแรงสัญญาณจะข้ามขั้น**
ซึ่งนาน ๆ ทีถึงจะเกิด ส่วนตัวเลข dBm ยังอยู่เพราะบรรทัดนั้นเขียนทับที่เดิมได้
แม้จะ `return` ไปแล้วก็ตาม ตรงกับที่ท่านเห็นพอดี คือเหลือแต่ตัวเลข ไม่มีขีด

แก้โดยให้ `drawStationTopBar()` เรียกตัวอัปเดตแบบ **บังคับวาด** แทนการวาดแบตเตอรี่เอง
ตัวอัปเดตวาดทั้งขีดสัญญาณและแบตเตอรี่ให้ในตัวอยู่แล้ว และยังตั้งค่าที่จำไว้
ให้ตรงกับของจริงไปพร้อมกัน ขีดจึงอยู่ครบทุกหน้าทุกครั้ง

### ตัดตัวเลขออกตามที่ต้องการ

ตัดทั้งตัวเลข `-63dB` และคำว่า `OFFLINE` ออกจากแถบบน เหลือแต่ขีดสัญญาณอย่างเดียว

ตอนขาดการเชื่อมต่อ เดิมอาศัยคำว่า OFFLINE เป็นตัวบอก พอตัดคำออกแล้ว
ขีดที่ว่างทั้งสี่จะดูไม่ต่างจาก "สัญญาณอ่อนมาก" จึงให้ **โครงขีดทั้งสี่เป็นสีแดง**
ตอนขาดการเชื่อมต่อ ต่างจากสีจางตามปกติ ดูแวบเดียวก็แยกออก

ค่า dBm ที่เป็นตัวเลขยังดูได้เหมือนเดิมที่หน้า SYSTEM แถว `Host link`
และสถานะออนไลน์ยังมีป้ายเต็มคำอยู่ที่หน้า 2 ไม่ได้หายไปไหน

### ตรวจฝั่งแม่ข่ายด้วยแล้ว

แถบบนของแม่ข่ายไม่มีขีดสัญญาณอยู่แล้ว มีแค่เลขหน้ากับแบตเตอรี่ จึงไม่มีอาการนี้
ส่วนขีดสัญญาณบนการ์ดร้านหน้า SHOPS นั้นเขียนไว้ถูกแล้ว เพราะหลังล้างพื้นมันสั่ง
`lastBars[i] = -1` บังคับให้วาดใหม่ ไม่ได้ปล่อยให้ค่าที่จำไว้ค้าง

---

## เปลี่ยนคำว่า Point เป็น Station ทั้งระบบ (Host 113.8.0 / Station 122.8.0)

ท่านทักว่าผมใช้คำว่า **Point** ทั้งที่ควรเป็น **station** หรือ **shop**

ถูกต้องครับ และที่แย่กว่านั้นคือมัน **ไม่ตรงกับอะไรเลยในระบบตัวเอง**
ชื่อไฟล์คือ `Canteen_Station_Client` ตัวแปรคือ `currentStationId` `stationNodes`
โปรโตคอลคือ `HostConfigPacket.stationId` แต่สิ่งที่ผู้ใช้เห็นบนจอกลับเขียนว่า Point
เปิดโค้ดกับมองจอแล้วเหมือนคนละระบบ

### แยกสองคำให้ชัด

| สิ่งที่หมายถึง | คำที่ใช้ | เหตุผล |
|---|---|---|
| ตัวเครื่องที่ตั้งอยู่หน้าร้าน | **Station** | ตรงกับชื่อไฟล์ ตัวแปร และโปรโตคอลทั้งหมด |
| ร้านค้าและผู้ประกอบการ | **Shop** | ใช้อยู่แล้วและไม่ได้แตะ |

หน้า SHOPS ของแม่ข่ายเห็นความต่างนี้ชัดที่สุด การ์ดแต่ละใบขึ้น **ชื่อร้าน** เป็นบรรทัดแรก
แล้วมี **Station N** เป็นบรรทัดรอง คือร้านชื่ออะไร และเครื่องตัวไหนตั้งอยู่ที่นั่น

### จุดที่เปลี่ยน

| ที่ | เดิม | ใหม่ |
|---|---|---|
| จอแม่ข่าย หน้า SHOPS | `Point N` | `Station N` |
| จอแม่ข่าย หน้า SYSTEM | `4 service points linked` | `4 stations linked` |
| จอแม่ข่าย รายการแตะบัตรล่าสุด | `Point N   card ...` | `Station N   card ...` |
| จอแม่ข่าย หน้าแจ้งผลเต็มจอ | `SERVICE POINT` / `Point N` | `STATION` / `Station N` |
| จอจุดบริการ แถบบนทุกหน้า | `POINT N` | `STATION N` |
| จอจุดบริการ หน้าบูต | `Service point` / `Point N` | `Canteen station` / `Station N` |
| จอจุดบริการ หน้า 2 | `THIS POINT` | `THIS STATION` |
| จอจุดบริการ หน้า SYSTEM | `Service point` / `Point N` | `Station` / `No. N` |
| จอจุดบริการ ตั้งหมายเลข | `POINT NUMBER` / `CHOOSE THIS POINT` | `STATION NUMBER` / `CHOOSE THIS STATION` |
| จอจุดบริการ หน้าแจ้งผล | `Point N` | `Station N` |
| หน้าเว็บ แท็บ Today | `Service points` / `Point N` | `Stations` / `Station N` |
| หน้าเว็บ แท็บ Settings | `Display on all service points` | `Display on all stations` |
| หน้าเว็บ ข้อความแจ้ง | `Service points updated` | `Stations updated` |

**คำไทยไม่ได้แตะเลย** ยังเป็น "จุดบริการ" และ "จุดที่" เหมือนเดิม เพราะคำไทยถูกอยู่แล้ว
ปัญหาอยู่ที่คำอังกฤษฝั่งเดียว

จุดหนึ่งที่ต้องขยับพิกัดด้วยคือชื่อบนการ์ดหน้าบูตของจุดบริการ
`Station N` กว้างกว่า `Point N` อยู่ 24 พิกเซล เลื่อนจุดเริ่มจาก x 85 เป็น 106
ข้อความจึงกลับมาอยู่กลางการ์ดเหมือนเดิม

### เก็บกวาดที่เจอระหว่างทาง

* โปรแกรมจำลองจอมีฟังก์ชัน `stOfflineSaved()` กับ `stOfflineRejected()` ตกค้างอยู่
  เป็นหน้าจอของฟีเจอร์คิวออฟไลน์ที่ถูกตัดออกจากเฟิร์มแวร์ไปตั้งแต่รุ่น 122.0.0
  ไม่ได้ถูกเรียกใช้และไม่เคยถูกเรนเดอร์ แต่ทำให้คนอ่านเข้าใจผิดว่ายังมีอยู่ ลบทิ้งแล้ว
* สคริปต์ถ่ายภาพหน้าเว็บในเครื่องมือของผมใช้ข้อมูลจำลองรูปแบบเก่า
  ซึ่งไม่ตรงกับที่ `/api/dashboard` ส่งจริงในรุ่นปัจจุบัน ภาพที่ได้จึงว่างเปล่าทั้งหน้า
  แก้ให้ตรงกับรูปแบบจริงแล้วถ่ายใหม่

ภาพในคู่มือถูกถ่ายใหม่ทั้งหมดแล้ว ทั้งจอ TFT สองฝั่งและหน้าเว็บ
ตัวตรวจทั้งแปดตัวผ่าน และการทดสอบระดับพิกเซลยังตรงกันทุกกรณี

---

## คำสั่งเปลี่ยนโหมดการแสดงผลไปถึงจุดบริการทันที (Host 113.7.0 / Station 122.7.0)

ท่านถามว่าสั่งให้พักหน้าจอหรือสลับสว่าง/มืดแล้ว **ฝั่งจุดบริการใช้เวลาราวห้าวินาที
กว่าจะเปลี่ยนตาม เป็นปกติหรือไม่ ทำไมไม่เปลี่ยนแบบเรียลไทม์**

**ไม่ปกติครับ เป็นบั๊ก** และไล่เจอสองจุดซ้อนกัน

### จุดที่หนึ่ง แพ็กเก็ตกระจายเสียงใส่ฟิลด์ไม่ครบ

`HostConfigPacket` มีเจ็ดฟิลด์ และในโค้ดมี `fillStationConfig()` เขียนไว้อยู่แล้ว
เป็นที่เดียวสำหรับประกอบแพ็กเก็ตนี้ พร้อมคอมเมนต์กำกับว่า
*"ชุดเดียวใช้ร่วมกันทุกที่ จะได้ไม่มีทางส่งค่าไม่ตรงกัน"*

แต่ `broadcastStationTheme()` **ไม่ได้เรียกมัน** กลับประกอบแพ็กเก็ตเองทีละฟิลด์
แล้วหยุดที่ `darkMode` ฟิลด์ที่เหลือจึงเป็นศูนย์ติดไปทุกครั้ง

| ฟิลด์ | ค่าที่ควรส่ง | ค่าที่ส่งจริง |
|---|---|---|
| `darkMode` | ค่าปัจจุบัน | ถูกต้อง |
| `screenOn` | ค่าปัจจุบัน | **0** |
| `screensaver` | ค่าปัจจุบัน | **0** |
| `modeSeq` | เลขลำดับปัจจุบัน | **0** |

ฝั่งจุดบริการกันคำสั่งซ้ำด้วย `modeSeq` เมื่อเลขไม่เคยขยับจากศูนย์
**คำสั่งพักหน้าจอจึงไม่เคยเดินทางมากับการกระจายเสียงเลยสักครั้ง**

แล้วมันไปถึงได้อย่างไร คำตอบคือทางอ้อม ทุกครั้งที่จุดบริการส่ง heartbeat มา
แม่ข่ายจะตอบกลับแล้วแนบ `sendStationTheme()` ไปด้วย ซึ่งเส้นทางนี้เรียก
`fillStationConfig()` อย่างถูกต้อง จุดบริการจึงได้ค่าจริง **แต่ต้องรอรอบ heartbeat**
ซึ่งตั้งไว้ที่ `6000 + หมายเลขจุด × 350` มิลลิวินาที ตรงกับห้าวินาทีที่ท่านจับได้พอดี

### จุดที่สอง สั่งเปลี่ยนธีมตอนพักหน้าจออยู่ จะถูกเตะออกจากหน้าพักจอ

ฝั่งจุดบริการ เมื่อธีมเปลี่ยนมันจะเรียก `showStationPage()` เพื่อวาดใหม่
แต่ฟังก์ชันนั้น **ตั้ง `currentState` เป็น STANDBY หรือ STATUS เสมอ**
ถ้าตอนนั้นกำลังพักหน้าจออยู่ การสั่งเปลี่ยนธีมอย่างเดียวจะเตะมันออกมา
และไม่กลับเข้าไปอีก เพราะ `modeSeq` ไม่ได้เปลี่ยน คำสั่งพักจอจึงไม่ถูกทำซ้ำ
แก้ให้วาดใหม่ตามหน้าที่แสดงอยู่จริง ไม่ไปเปลี่ยนสถานะของเครื่อง

### วิธีแก้เรื่องความเร็ว

* `broadcastStationTheme()` เรียก `fillStationConfig()` เหมือนทุกที่ ค่าครบและตรงกันเสมอ
* `announceDisplayMode()` **ส่งยูนิแคสต์ถึงทุกจุดบริการที่แม่ข่ายรู้จัก MAC แล้วก่อน**
  แล้วค่อยกระจายเสียงตามไป ทำสามรอบเหมือนเดิม

ที่เลือกยูนิแคสต์เป็นหลักเพราะ **ชั้นยูนิแคสต์ของ ESP-NOW มีการตอบรับและส่งซ้ำให้ในตัว**
ต่างจากการกระจายเสียงที่ยิงออกไปแล้วไม่มีใครยืนยัน และที่สำคัญกว่านั้นคือ
มันเป็นเส้นทางเดียวกับที่ใช้ตอบ heartbeat ซึ่ง **พิสูจน์แล้วจากอาการของท่านเองว่าถึงแน่นอน**
ส่วนการกระจายเสียงยังคงไว้ เผื่อจุดบริการที่เพิ่งเปิดเครื่องและยังไม่เคยส่งอะไรมา
แม่ข่ายจึงยังไม่รู้ MAC ของมัน

### ตัวตรวจใหม่ และตัวตรวจที่ไม่ได้ทำงาน

เพิ่ม `packetcheck.py` บังคับว่าทุกที่ที่ประกอบ `HostConfigPacket` ต้องเรียก
`fillStationConfig()` ห้ามตั้ง `msgType = MSG_CONFIG` เองที่อื่น

ระหว่างพิสูจน์ว่ามันจับได้จริง **พบว่าตัวตรวจใหม่นี้ไม่ได้ทำงานเลย** มันขึ้นว่า OK
ทั้งที่รันกับคอมมิตที่เสียจริง ต้นเหตุคือมัน `import body_of` มาจาก `privacycheck.py`
ซึ่ง **ไม่มี `if __name__ == '__main__'`** พอถูก import ไฟล์นั้นจึงรันตัวเองแล้ว
`sys.exit()` ทิ้งทันที ผลที่ผมเห็นว่า "OK" คือผลของ `privacycheck` ไม่ใช่ของตัวใหม่

แก้สองชั้น

* ห่อเนื้อการตรวจของ `privacycheck.py` ไว้ใน `main()` แล้วเรียกผ่าน `__main__` guard
  แบบเดียวกับ `bracecheck.py` ที่ทำถูกอยู่แล้ว
* เพิ่ม `runall.py` รันตัวตรวจทุกตัวเป็น **โปรเซสแยก** ไม่ใช่การ import
  และถือว่า **ตัวตรวจที่ไม่พิมพ์อะไรออกมาเลยคือไม่ผ่าน** ไม่ใช่ผ่านเงียบ ๆ
  ต่อไปสั่ง `python3 tools/proto-check/runall.py` ครั้งเดียวจบ

หลังแก้แล้ว `packetcheck.py` ฟ้องบรรทัดที่ 580 ของคอมมิตที่เสีย ตรงจุดพอดี

### ที่ยังต้องรบกวนท่านยืนยัน

ผมพิสูจน์จากโค้ดได้เต็มร้อยว่า **คำสั่งพักหน้าจอ** ไปไม่ถึงทางการกระจายเสียง
ส่วน **การสลับสว่าง/มืด** นั้นค่าที่ส่งถูกต้องอยู่แล้ว ตามทฤษฎีจึงควรเปลี่ยนทันที
แต่ท่านบอกว่าช้าเท่ากัน แปลว่าแพ็กเก็ตกระจายเสียงน่าจะไปไม่ถึงตั้งแต่แรก
ซึ่งเป็นเรื่องคลื่นวิทยุที่ผมวัดจากตรงนี้ไม่ได้

การแก้ครั้งนี้ครอบคลุมทั้งสองกรณี เพราะย้ายมาใช้เส้นทางยูนิแคสต์ที่ถึงแน่นอน
**รบกวนท่านลองจับเวลาอีกครั้งหลังแฟลช** ทั้งการพักหน้าจอและการสลับสี

---

## คลื่นของสัญลักษณ์แตะบัตรไม่ขึ้นบนจอจริง (Host 113.6.0 / Station 122.6.2)

ท่านแจ้งว่าคอมไพล์ผ่านแล้ว แต่ **"ไอคอนไม่สมบูรณ์ ขาดตัวเส้นสีเขียวที่แสดงถึงคลื่นจากการ์ด"**
คือตัวบัตรขึ้นครบ แต่คลื่นสามชั้นหายไปทั้งหมด

### ต้นเหตุ

Adafruit GFX แบ่งฟังก์ชันวาดเป็นสองพวก ซึ่งผมไม่ได้แยกแยะตอนเขียน

| พวก | ตัวอย่าง | เรียกเดี่ยว ๆ ได้ไหม |
|---|---|---|
| มี `startWrite()`/`endWrite()` ในตัว | `drawPixel` `fillRect` `drawRoundRect` `drawFastHLine` | ได้ |
| ไม่มี เรียก `writePixel()` ตรง ๆ | `writePixel` `writeLine` **`drawCircleHelper`** `fillCircleHelper` | **ไม่ได้** |

ผมดาวน์โหลดซอร์สของไลบรารีมาอ่านยืนยันแล้ว `drawCircleHelper()` ไม่มี `startWrite()`
เพราะมันถูกออกแบบมาให้ถูกเรียก **จากข้างใน** `drawRoundRect()` ซึ่ง `startWrite()` ไว้ก่อนแล้ว

เมื่อเราเรียกมันเดี่ยว ๆ ทรานแซกชัน SPI ไม่ถูกเปิด **ขา CS ไม่ถูกดึงลง จอจึงไม่ได้รับข้อมูลเลย**
นี่คือเหตุผลที่ตัวบัตรขึ้นครบ เพราะวาดด้วย `drawRoundRect` `fillRoundRect` `drawFastHLine`
ซึ่งอยู่ในพวกแรกทั้งหมด ส่วนคลื่นที่วาดด้วย `drawCircleHelper` หายไปทั้งเส้น
ตรงกับอาการที่ท่านเห็นพอดี

โค้ดชุดนี้ลอกมาจากรุ่น 118.1.0 ทั้งดุ้น แปลว่า **บั๊กนี้มีมาตั้งแต่ตอนนั้น**
คลื่นน่าจะไม่เคยขึ้นบนเครื่องจริงเลย

### วิธีแก้

เขียนวงกลมครึ่งขวาเองด้วยอัลกอริทึมของ Bresenham ชุดเดียวกับไลบรารี
แต่ลงจุดด้วย `tft.drawPixel()` ซึ่งเปิดและปิดทรานแซกชันให้เองทุกจุด
เลือกทางนี้แทนการครอบด้วย `startWrite()`/`endWrite()` เพราะไม่ต้องไปพึ่งรายละเอียด
ภายในของไลบรารีที่อาจเปลี่ยนในรุ่นถัดไป และเพิ่ม **จุดขวาสุด** ที่ลูปของไลบรารีข้ามไป
ปลายโค้งจึงไม่ขาดหนึ่งพิกเซลอย่างของเดิม

วาดทั้งหมดราวสามร้อยจุด เฉพาะตอนเข้าหน้าและตอนสถานะเปลี่ยน ไม่ใช่ทุกวินาที

### ทำไมตัวตรวจกับภาพตัวอย่างไม่เห็นบั๊กนี้

**ภาพตัวอย่างคือตัวการ** โปรแกรมจำลองของผมวาดส่วนโค้งด้วย `g.arc()` ของ canvas
ซึ่ง **วาดสำเร็จเสมอ** ภาพจึงออกมาสวยและผมก็เชื่อว่ามันถูก
บทเรียนคือโปรแกรมจำลองที่ "ใจดีเกินจริง" อันตรายกว่าการไม่มีโปรแกรมจำลองเลย
เพราะมันให้ความมั่นใจปลอม ๆ

แก้สองอย่าง

* เปลี่ยนโปรแกรมจำลองมาลงจุดทีละพิกเซลด้วย Bresenham ชุดเดียวกับเฟิร์มแวร์
  ภาพตัวอย่างตอนนี้จึงเป็นพิกเซลชุดเดียวกับที่จอได้รับจริง ขอบหยักเหมือนของจริง
* เพิ่ม `tools/proto-check/gfxcheck.py` เป็นตัวที่แปด ฟ้องทันทีถ้ามีการเรียก
  ฟังก์ชันพวกที่สองโดยไม่มี `startWrite()` เปิดไว้ พิสูจน์แล้วสองทาง
  คือรันกับคอมมิตที่เสียจริง จับได้ทั้งสองบรรทัด และลองครอบด้วย
  `startWrite()`/`endWrite()` ดู ตัวตรวจก็ไม่ฟ้องผิด

บั๊กชนิดนี้ร้ายเป็นพิเศษ เพราะ **คอมไพล์ผ่าน ไม่มีคำเตือน แต่ภาพไม่ขึ้นเงียบ ๆ**
ต่อไปจะถูกจับได้ตั้งแต่ก่อน commit

---

## แก้คอมไพล์ไม่ผ่าน และอุดช่องที่ตัวตรวจมองไม่เห็น (Host 113.6.0 / Station 122.6.1)

ท่านส่งข้อความจากคอมไพเลอร์มาว่า

```
StationScreen.h:393: error: 'drawStandbyReadyState' was not declared in this scope
```

ผมพลาดเองครับ ตอนแทรกฟังก์ชันใหม่ ผมวาง `drawStandbyReadyState()` ไว้ติดกับ
`displayTapCardStandby()` ที่บรรทัด 503 เพราะมันเป็นเรื่องเดียวกัน
แต่ลืมไปว่า `refreshStationLiveValues()` ที่บรรทัด 393 ก็เรียกใช้มันด้วย
ซึ่งอยู่ **เหนือ** จุดที่นิยามไว้ C++ อ่านจากบนลงล่าง จึงยังไม่รู้จักชื่อนี้

แก้โดย **ย้ายนิยามขึ้นไปไว้ใต้ `drawRfidTapIcon()` ทันที** ซึ่งอยู่เหนือทั้งสองจุดที่เรียก
เลือกย้ายแทนการเพิ่มบรรทัดประกาศล่วงหน้า เพราะสองฟังก์ชันนี้เป็นเรื่องเดียวกัน
คือรูปกับป้ายของมัน อยู่ติดกันแล้วอ่านง่ายกว่า และไม่ต้องดูแลสองที่

### เรื่องที่สำคัญกว่าตัวบั๊ก

บั๊กนี้ **ตัวตรวจทั้งหกตัวของผมปล่อยผ่านหมด** ทั้งที่มันเป็นบั๊กชนิดเดียวกับที่เคยเกิด
มาแล้วสองครั้งในโปรเจกต์นี้ สาเหตุคือ `splitcheck.py` ที่เขียนไว้ตอนนั้น
ดูแลเฉพาะ **รอยต่อระหว่าง `.ino` กับ `.h`** คือกรณีที่โค้ดใน `.ino` เรียกฟังก์ชันที่ย้ายไปอยู่ใน `.h`
มันจึงมองไม่เห็นกรณีที่ทั้งผู้เรียกและผู้ถูกเรียกอยู่ในไฟล์ `.h` เดียวกัน

เพิ่ม `tools/proto-check/ordercheck.py` เป็นตัวที่เจ็ด ตรวจกติกาเดียวกันแต่ทั่วถึงกว่า

* ไล่ทุกการเรียกฟังก์ชันในทุกไฟล์ แล้วเทียบกับบรรทัดที่นิยามไว้
* ถ้าถูกเรียกก่อนถูกนิยาม และไม่มีบรรทัดประกาศล่วงหน้าอยู่ข้างบน ถือว่าผิด
* มองเห็น `.ino` กับ `.h` ที่ถูก `#include` **เป็นไฟล์เดียวกันแบบที่คอมไพเลอร์เห็น**
  บรรทัดประกาศล่วงหน้าที่อยู่ใน `.ino` เหนือ `#include` จึงนับว่าใช้ได้
  รุ่นแรกของตัวตรวจที่ผมเขียนยังไม่ได้มองแบบนี้ จึงฟ้องผิดห้าจุดที่ไม่ได้ผิดจริง

พิสูจน์ว่าจับได้จริงสองทาง โดยรันกับสำเนาของคอมมิตที่ท่านคอมไพล์ไม่ผ่าน

```
บรรทัด 393 เรียก drawStandbyReadyState() ซึ่งนิยามอยู่บรรทัด 503
คือต่ำกว่าจุดที่เรียก และไม่มีบรรทัดประกาศล่วงหน้าอยู่ข้างบน
```

ตรงกับที่คอมไพเลอร์ฟ้องทั้งชื่อฟังก์ชันและเลขบรรทัด แล้วลองอีกทางหนึ่ง
คือลบบรรทัดประกาศล่วงหน้าของ `displayTapCardStandby()` ออกจาก `.ino`
ตัวตรวจก็จับได้เช่นกัน

**ขออภัยที่ต้องรบกวนท่านคอมไพล์ให้อีกรอบครับ** ตราบใดที่ผมยังเข้า
`downloads.arduino.cc` ไม่ได้ ท่านคือคนเดียวที่ยืนยันได้ว่าโค้ดคอมไพล์ผ่าน
สิ่งที่ผมทำได้คือทำให้ความผิดพลาดแต่ละแบบที่เกิดขึ้นจริง ถูกจับได้ตั้งแต่ก่อนส่งถึงมือท่าน

---

## คืนสัญลักษณ์แตะบัตรกลับมา และทำให้มันบอกความพร้อมจริง (Host 113.6.0 / Station 122.6.0)

ท่านทักว่า **"ฝั่ง Station ท่านได้ลบสัญลักษณ์ในการแตะบัตรไปไหม ผมยังต้องการสัญลักษณ์นั่นครับ
เพื่อผู้ใช้งานรู้ว่าเครื่องพร้อมให้บริการ"**

ถูกต้องครับ **หายไปจริง** สัญลักษณ์นี้เคยมีตั้งแต่รุ่น 118.1.0 แล้วหายไปตอนที่เริ่มใหม่
จากต้นฉบับในรุ่น 122.0.0 หน้าแรกจึงเหลือแต่ตัวหนังสือ นิสิตที่เดินมาถึงไม่มีอะไรบอกว่า
เครื่องพร้อมรับบัตรอยู่หรือเปล่า ผมเอาโค้ดเดิมกลับมาทั้งดุ้น

### แต่เอากลับมาเฉย ๆ ยังไม่ตอบโจทย์ที่ท่านบอก

ท่านบอกเหตุผลไว้ชัดว่าต้องการให้ **"ผู้ใช้งานรู้ว่าเครื่องพร้อมให้บริการ"**
ของเดิมเป็นรูปนิ่ง คลื่นเป็นสีเขียวตลอดเวลา ถ้าเครื่องอ่านบัตรค้างขึ้นมา
สัญลักษณ์ก็ยังเขียวอยู่ดี นิสิตแตะแล้วไม่มีอะไรเกิดขึ้น แล้วก็ยืนงงกันอยู่ตรงนั้น

ในโค้ดมี `checkRC522()` ที่อ่านทะเบียนรุ่นของ RC522 ทุกสิบวินาทีอยู่แล้ว
แต่ของเดิม **ถ้าอ่านไม่ได้ก็แค่สั่งเริ่มใหม่แล้วจบ** ไม่มีใครรู้ว่าสำเร็จหรือไม่
และไม่มีอะไรบนหน้าจอบอก ผมจึงต่อสองส่วนนี้เข้าด้วยกัน

* อ่านทะเบียนรุ่น **ซ้ำอีกครั้งหลังสั่งเริ่มใหม่** แล้วเก็บผลไว้ใน `isReaderReady`
* อ่านหนึ่งครั้งตอนบูตด้วย สัญลักษณ์จะได้ถูกต้องตั้งแต่วินาทีแรก ไม่ต้องรอรอบตรวจรอบแรก
* คลื่นของสัญลักษณ์ **เขียว = เครื่องอ่านตอบอยู่**, **แดง = ไม่ตอบ**
  และบรรทัดใต้ข้อความเปลี่ยนจาก `Free meal  35 baht  once a day`
  เป็น `Card reader not responding - call staff`

สัญลักษณ์จึงไม่ใช่ของประดับ แต่เป็นสิ่งที่เจ้าหน้าที่มองแวบเดียวก็รู้ว่าเครื่องไหนต้องไปดู

### จัดหน้าใหม่ให้มีที่ว่างพอ

การ์ดใบบนมีที่ว่างเหลืออยู่แล้วราว 36 พิกเซลด้านล่าง จัดใหม่เป็น

| ส่วน | ตำแหน่ง |
|---|---|
| สัญลักษณ์แตะบัตร | กึ่งกลางที่ (160, 72) กินพื้นที่ 72×47 พิกเซล |
| `TAP YOUR CARD` | y 100 สูง 32 |
| บรรทัดคำอธิบาย | y 139 |

บรรทัดคำอธิบายเปลี่ยนจาก `drawFitCenteredText()` เป็น `drawCenteredText()`
เพราะข้อความสองแบบยาวไม่เท่ากัน ตัวเดิมไม่ลบของเก่า สลับข้อความแล้วจะมีหางค้าง

### ตรวจแล้วว่าไม่กลับไปกะพริบ

จุดที่ผมระวังที่สุดคือสัญลักษณ์นี้วาดด้วยเส้นโค้ง ซึ่งวาดทับเส้นโค้งเดิมไม่มิด
สีเก่าจะค้างตามขอบเหมือนที่เคยเจอกับป้าย ONLINE/OFFLINE และแถบความคืบหน้า
จึงต้องล้างพื้นก่อน — แต่การล้างพื้นคือต้นเหตุของการกะพริบที่เพิ่งแก้ไป

ทางออกคือ **ล้างพื้นเฉพาะตอนสถานะเปลี่ยน** ซึ่งนาน ๆ ครั้ง ไม่ใช่ทุกวินาที
เพิ่มกรณีทดสอบสองกรณีใน `partial_test.js` คือตอนเครื่องอ่านล้ม และตอนกลับมาปกติ
ทั้งสองกรณี **ตรงกับการวาดใหม่ทั้งหน้าทุกพิกเซล ทั้งโหมดมืดและโหมดสว่าง**
ส่วนเส้นทางที่วาดทุกวินาที (นาฬิกา) ยังล้างพื้น **0 ครั้ง** เหมือนเดิม

ตัวตรวจคอมเมนต์ยังจับได้ด้วยว่าผมเผลอใช้คำว่า "คืนกลับ" ในป้ายเวอร์ชัน
ซึ่งไม่อยู่ในคำที่มาตรฐานอนุญาต (เพิ่ม/แก้/ย้าย) แก้เป็น "เพิ่ม" แล้ว

**ยังคอมไพล์ไม่ได้เหมือนเดิมครับ** รบกวนท่านแฟลชแล้วดูว่าคลื่นเขียวขึ้นถูกต้องไหม
และถ้าอยากลองกรณีแดง ให้ถอดสาย SDA ของ RC522 ออกสักสิบวินาที

---

## จอสาธารณะบนทีวีในโรงอาหาร (Host 113.6.0 / Station 122.5.0)

ท่านถามว่า **"เรายังคงมีการแสดงผลแบบไม่ต้องล็อกอินไหมครับ เพื่อไปเปิดบนหน้าจอทีวีจอใหญ่"**
คำตอบตอนนั้นคือ **ไม่มีแล้ว** — เฟิร์มแวร์รุ่นเก่ามีหน้านี้อยู่ แต่ตอนที่เขียนแดชบอร์ดใหม่
ในรุ่น 113.0.0 หน้านี้หายไป ทุกเส้นทางกลายเป็นต้องเข้าสู่ระบบหมด
ท่านจึงสั่งว่า **"เขียนใหม่ให้เข้าชุดกับหน้าเว็บปัจจุบัน"**

### หน้าใหม่

เปิดที่ `http://192.168.4.1/display` ไม่ต้องเข้าสู่ระบบ ปล่อยค้างเต็มจอได้ทั้งวัน
ใช้ชุดสี ตัวอักษร และรูปแบบการ์ดเดียวกับแดชบอร์ด แต่จัดวางใหม่ทั้งหมด
สำหรับคนที่ยืนอ่านจากอีกฝั่งของห้อง ไม่ใช่คนที่นั่งหน้าจอ

| ส่วน | เนื้อหา |
|---|---|
| แถบบน | ชื่อโรงอาหาร นาฬิกาเดินวินาที วันที่ และป้ายเปิด/ปิดบริการ |
| ตัวเลขสามช่อง | จ่ายไปกี่จาน เป็นเงินกี่บาท เหลืออีกกี่คน |
| ยอดขายรายร้าน | ชื่อร้าน จำนวนจาน จำนวนเงิน และแท่งเทียบสัดส่วน |
| รายการล่าสุด | แปดรายการหลังสุดที่ผ่านแล้ว |

ไทยกับอังกฤษขึ้นพร้อมกัน ไม่มีปุ่มสลับภาษา เพราะไม่มีใครเดินไปกดปุ่มบนจอทีวี
ขนาดตัวอักษรทุกจุดคิดจากหน่วย `vmin` จอ 1920×1080 กับ 1366×768
จึงได้สัดส่วนเดียวกันโดยไม่ต้องมีจุดตัด

### หน้านี้เปิดให้ทุกคนเห็น จึงต้องกันข้อมูลส่วนบุคคลตั้งแต่ต้นทาง

ของเดิมที่เคยมีในเฟิร์มแวร์รุ่นเก่านั้นดึงจาก API ตัวเดียวกับแดชบอร์ด
ซึ่งมีทั้งชื่อนิสิตและหมายเลขบัตร หน้าใหม่จึงไม่ได้ใช้ `/api/dashboard`
แต่มี `/api/board` เป็นของตัวเอง ที่ **ประกอบข้อมูลขึ้นมาใหม่เฉพาะที่ยอมให้เห็น**
ไม่ใช่การซ่อนด้วย CSS ที่กด View Source แล้วเห็น

* รหัสนิสิตถูกตัดเหลือห้าหลักแรกตั้งแต่ตอนบันทึกลงคิว ไม่ใช่ตอนส่งออก
  ตัวเต็มไม่เคยออกจากเครื่องทางเส้นทางนี้เลย
* ไม่มีชื่อ ไม่มีหมายเลขบัตร ไม่มีเลขอ้างอิง
* ไม่มีอุณหภูมิ หน่วยความจำ หรือ MAC ของเครื่องแม่ข่าย
* มีเฉพาะรายการที่ผ่าน รายการที่ถูกปฏิเสธหรือซ้ำไม่ขึ้นบนจอนี้

คิวเก็บแค่แปดรายการหลังสุดใน `PublicFeedItem` ซึ่งเป็นโครงสร้างแยกต่างหาก
ขนาดคงที่ มีแค่สามฟิลด์ คือรหัสที่ปิดบังแล้ว เวลา และเลขร้าน
โครงสร้างนี้ **ไม่มีที่ให้ใส่ชื่อหรือหมายเลขบัตรได้เลยแม้จะเผลอ** และถูกล้างพร้อมกับ
การปิดยอดประจำวันที่ `/reset`

### ตัวตรวจใหม่: `privacycheck.py`

ข้อกำหนดข้างบนจะอยู่ได้ไม่นานถ้าอาศัยความจำอย่างเดียว จึงเขียนตัวตรวจไว้ ทำสองอย่าง

1. อ่านตัวฟังก์ชัน `handlePublicBoard()` แล้วฟ้องถ้าพบการอ้างถึงฟิลด์ต้องห้าม
   ไม่ว่าจะเป็น `fullName` `uid` `refNo` หรือฟังก์ชันอ่านข้อมูลฮาร์ดแวร์
2. ไล่ `server.on()` ทุกบรรทัด แล้วฟ้องถ้ามีเส้นทางไหนที่ไม่ได้เรียก
   `isAuthenticated()` และไม่ได้อยู่ในรายการสาธารณะที่ประกาศไว้
   ใครเพิ่มเส้นทางใหม่แล้วลืมใส่การตรวจสิทธิ์จะถูกจับได้ทันที

ตอนรันครั้งแรกมันฟ้อง `/upload` ตรวจแล้วพบว่า **เป็นข้อบกพร่องของตัวตรวจเอง**
ไม่ใช่ช่องโหว่ — `/upload` ลงทะเบียนด้วยรูปแบบสี่อาร์กิวเมนต์
`server.on(path, method, handler, upload)` ซึ่งมีตัวจัดการสองตัว
ตัวตรวจรุ่นแรกจับได้เฉพาะตัวเดียวที่ตามด้วยวงเล็บปิด จึงมองไม่เห็นทั้งคู่
แก้ให้อ่านตัวจัดการทุกตัวแล้วบังคับว่า **ต้องตรวจสิทธิ์ทุกตัว** ซึ่งเข้มกว่าเดิม
ของจริงทั้ง `mergeImportedStudents()` และ `handleFileUpload()` ตรวจสิทธิ์อยู่แล้วทั้งคู่
และพิสูจน์ว่าตัวตรวจจับได้จริงโดยลองถอดการตรวจสิทธิ์ออกจากสำเนาแล้วรันซ้ำ

### ตรวจก่อนส่ง

ผมยังคอมไพล์ไม่ได้ (`downloads.arduino.cc` ตอบ 403 ผ่านพร็อกซี) จึงตรวจเท่าที่ทำได้

* ตัวตรวจทั้งหกตัวผ่านหมด
* ดึง `DISPLAY_HTML` ออกจาก `WebDashboard.h` กลับมาเทียบกับต้นฉบับ **ตรงกันทุกไบต์**
  8,560 ไบต์ แล้วเรนเดอร์จากไฟล์ที่ดึงออกมานั้น ไม่ใช่จากต้นฉบับ
* เรนเดอร์สี่กรณีด้วย Chromium จริง: 1920×1080, 1366×768, โหมดสว่าง
  และกรณียังไม่มีใครมารับ **ไม่มี error และไม่มีการล้นจอทั้งแนวตั้งและแนวนอน**
* ภาพทั้งสี่อยู่ใน `docs/interface/` และในหน้ารวมภาพแล้ว

**ยังต้องให้ท่านทดสอบบนเครื่องจริง** โดยเฉพาะว่าเบราว์เซอร์ของทีวีรุ่นที่ใช้
รองรับหน่วย `vmin` และ `fetch` หรือไม่ ถ้าเป็นทีวีรุ่นเก่ามากอาจต้องปรับ

---

## จัดตำแหน่งตัวอักษรบนจอให้ถูกต้อง (Host 113.5.0 / Station 122.5.0)

ท่านแจ้งว่าหลังแฟลชรุ่นก่อน **อักษรซ้อนหายไปแล้ว และการกะพริบดีขึ้น**
แต่บรรทัดสีเขียวบนหน้าพักจอไม่ได้อยู่กึ่งกลางเหมือนนาฬิกากับวันที่

สาเหตุคือตอนผมเลิกล้างพื้น ผมเปลี่ยนบรรทัดที่เคย **คำนวณกึ่งกลางเอง**
ให้เป็น `drawFixedText()` ซึ่งเติมช่องว่าง **ท้ายอย่างเดียว** ข้อความจึงชิดซ้าย
ของช่องเสมอ และเยื้องไปทางซ้ายเมื่อเทียบกับบรรทัดอื่น

### วิธีแก้

เพิ่ม `drawCenteredText()` ที่เติมช่องว่าง **ทั้งสองข้าง** ให้ช่องกว้างคงที่
ข้อความจึงอยู่กึ่งกลางจริง และช่องว่างก็ลบของเดิมที่ยาวกว่าไปพร้อมกัน
ยังไม่ต้องล้างพื้น จึงไม่กลับไปกะพริบอีก

### ตรวจหน้าอื่นแล้วแก้เพิ่มอีกสี่จุด

| หน้า | แก้อะไร |
|---|---|
| **หน้าพักจอ** ทั้งสองฝั่ง | นาฬิกา วันที่ ยอดวันนี้ และแบตเตอรี่ จัดกึ่งกลางที่ x = 160 เหมือนกันหมด |
| **หน้า TODAY** ของแม่ข่าย | วันที่กับช่วงเวลาให้บริการอยู่บรรทัดฐานเดียวกัน จากเดิมเหลื่อมกัน 4 พิกเซล |
| **หน้า 2 ของจุดบริการ** | คำว่า `baht` เคยอยู่ที่ตำแหน่งคงที่ทางขวาของตัวเลข พอตัวเลขสั้นก็เหลือช่องว่างกลางอากาศ ย้ายลงมาอยู่ใต้ตัวเลข ชิดซ้ายเหมือนบรรทัดอื่นในการ์ด |
| **หน้าแรกของจุดบริการ** | นาฬิกาเคยลอยอยู่ระหว่างหัวข้อกับตัวเลข ย้ายให้อยู่บรรทัดเดียวกับตัวเลขยอด |
| **หน้า SYSTEM ของจุดบริการ** | หกแถวกองอยู่ครึ่งบนของการ์ด เหลือที่ว่างข้างล่างเกือบหนึ่งในสี่ กระจายใหม่ให้เต็ม |

ส่วนหน้าอื่นที่ตรวจแล้วไม่ต้องแก้: หน้า SHOPS ของแม่ข่าย (คอลัมน์ป้ายกำกับตรงกันทั้งสี่การ์ด),
หน้า SYSTEM ของแม่ข่าย, หน้าแจ้งผลการแตะบัตร และหน้าแจ้งสถานะ Wi-Fi ตอนบูต
ซึ่งใช้การจัดกึ่งกลางของตัวเองอยู่แล้วและวาดครั้งเดียว

ตัวทดสอบเทียบภาพ 20 กรณียังผ่านครบ ทั้งเรื่องไม่มีเศษพิกเซลค้าง
และเรื่องไม่ล้างพื้นบนบรรทัดที่วาดทุกวินาที

## แก้อักษรซ้อนและภาษาไทยอ่านไม่ออกบนจอ TFT (Host 113.4.0 / Station 122.4.0)

สองอาการที่ท่านเจอมาจาก**ต้นเหตุเดียวกัน**

ฟอนต์ในตัวของ Adafruit GFX มีแต่ ASCII ไบต์ตั้งแต่ `0x80` ขึ้นไปถูกวาดเป็นสัญลักษณ์มั่ว
และ **ภาษาไทยหนึ่งตัวกินสามไบต์** จึงกลายเป็นตัวประหลาดสามตัว
ที่สำคัญกว่านั้นคือ **มันกินที่กว้างกว่าที่โค้ดนับไว้สามเท่า** ข้อความจึงล้นกรอบ
ไปทับของข้างเคียง นั่นคืออักษรซ้อนที่ท่านเห็น

### ต้นตอที่แท้จริง: ค่าเริ่มต้นในโค้ดเป็นภาษาไทย

```c
Shop shops[4] = {
  {"ร้านที่ 1", "นางสาวณัฐกฤตา สุพิทิพย์"},   // <-- ชื่อนี้ถูกส่งไปวาดบนจอตรง ๆ
```

จอจึงขึ้นมั่ว **ตั้งแต่ยังไม่ได้ตั้งค่าอะไรเลย** ไม่ต้องรอให้ใครพิมพ์ชื่อไทยเข้าไป
เป็นของที่ติดมาตั้งแต่ต้นฉบับ 107.0.1

### วิธีแก้ สามชั้น

**ชั้นที่หนึ่ง — ชื่อร้านมีช่องสำหรับจอโดยเฉพาะ**
เพิ่มช่องที่สามในแท็บ Shops บนหน้าเว็บ สำหรับตั้งชื่อสั้น ๆ ภาษาอังกฤษที่จะขึ้นบนจอ
ชื่อจริงภาษาไทยยังอยู่ครบ ใช้บนหน้าเว็บและในไฟล์รายงานเหมือนเดิม
ถ้าเว้นว่างไว้ จอจะถอยไปใช้ส่วนที่เป็น ASCII ของชื่อจริง แล้วถอยไปที่ `Shop 1` ถึง `Shop 4`

**ชั้นที่สอง — กรองทุกข้อความก่อนถึงจอ**
เพิ่ม `asciiOnly()` ทั้งสองฝั่ง ใช้กับทุกข้อความที่มาจากผู้ใช้หรือจากฐานข้อมูล
ทั้งชื่อร้าน ชื่อนิสิต และรหัสนิสิต **จอจะไม่มีวันได้รับข้อความที่ฟอนต์วาดไม่ได้อีก**

**ชั้นที่สาม — ทุกช่องตัดความยาวไม่ให้ล้น**
`drawFixedText()` เปลี่ยนจาก `%-*s` (เติมให้ครบแต่ยาวเกินได้) เป็น `%-*.*s`
(ตัดให้พอดีเสมอ) ต่อให้มีข้อความยาวผิดคาดหลุดมาได้ ก็ล้นไปทับช่องข้างเคียงไม่ได้
ทุกช่องถูกกำหนดความกว้างเผื่อไว้แล้วให้ค่าจริงยาวไม่ถึง

### เพิ่มตัวตรวจ `tools/proto-check/asciicheck.py`

ตรวจสองข้อในไฟล์ที่วาดจอ: สตริงในโค้ดต้องเป็น ASCII ล้วน
และตัวแปรที่มาจากผู้ใช้ต้องผ่าน `asciiOnly()` ก่อนวาด

ทดสอบด้วยการใส่บั๊กทั้งสองแบบกลับเข้าไปในไฟล์สำเนา ตัวตรวจชี้ได้ถูกทั้งคู่

### ข้อจำกัดที่ต้องบอกตรง ๆ

การกรองแบบนี้คือ **ตัดตัวอักษรไทยทิ้ง ไม่ใช่การแปลงเป็นอักษรโรมัน**
ถ้าอยากให้จอแสดงชื่อร้านเป็นไทยจริง ๆ ต้องฝังฟอนต์บิตแมปภาษาไทยเข้าไป
พร้อมเขียนตัวจัดวางสระและวรรณยุกต์เอง ซึ่งเป็นงานคนละขนาดและกินแฟลชเพิ่มมาก
ถ้าท่านต้องการจริง ๆ บอกได้ครับ

## แก้จอกะพริบทุกวินาที (Host 113.3.0 / Station 122.3.0)

รอบที่แล้วผมแก้ผิดจุด ไปลดจำนวนครั้งที่วาดซ้ำ ทั้งที่**ต้นเหตุไม่ใช่การวาดบ่อย
แต่เป็นวิธีวาด** โค้ดเดิมทำตามลำดับ "ล้างพื้นด้วย `fillRect` แล้วค่อยเขียนตัวอักษร"
ระหว่างสองจังหวะนั้นจอว่างเปล่าจริง ๆ ตาคนจึงเห็นเป็นการกะพริบ
และเพราะนาฬิกาเดินทุกวินาที จอจึงกะพริบทุกวินาที

### วิธีแก้ที่ถูกต้อง

Adafruit GFX ลงสีพื้นให้ทุกตัวอักษรอยู่แล้วเมื่อกำหนดสีพื้นไว้ด้วย
(`setTextColor` แบบสองอาร์กิวเมนต์) ข้อความใหม่จึงเขียนทับของเดิมได้ในจังหวะเดียว
**ไม่ต้องล้างพื้นเลย** ไม่มีช่วงที่จอว่าง จึงไม่มีอะไรให้เห็นเป็นการกะพริบ

เงื่อนไขเดียวคือความยาวต้องคงที่ จึงเพิ่ม `drawFixedText()` ไว้ทั้งสองฝั่ง
ซึ่งเติมช่องว่างท้ายข้อความให้ครบความกว้างที่กำหนดเสมอ ไม่งั้นตัวอักษรเก่าที่ยาวกว่า
จะค้างอยู่ เช่น 187 เปลี่ยนเป็น 9 แล้วเหลือ 87 ค้างจอ

ใช้ `%-*s` ซึ่งเติมให้ครบแต่**ไม่ตัดทิ้ง** ค่าที่ยาวเกินคาดจึงยังแสดงครบ ไม่โกหกตัวเลข

### จุดที่แก้

| ที่ | เดิม |
|---|---|
| จอแม่ข่าย หน้า TODAY | ล้างพื้นนาฬิกา 196×34 ทุกวินาที |
| จอแม่ข่าย หน้า SHOPS | ล้างแถวสถานะกับแถวสัญญาณของทั้งสี่การ์ดทุกวินาที แม้ค่าไม่เปลี่ยน |
| จอแม่ข่าย หน้า SYSTEM | ล้างแถวอุณหภูมิและหน่วยความจำทุกครั้งที่ค่าขยับ |
| จอแม่ข่าย หน้าพักจอ | ล้างพื้นนาฬิกาตัวโต 260×52 ทุกวินาที |
| จอจุดบริการ ทุกหน้า | ล้างพื้นก่อนเขียนทุกจุด |
| จอจุดบริการ หน้า SYSTEM | อัปเดตทุกครึ่งวินาที จึงกะพริบถี่ที่สุด |
| จอจุดบริการ แถบบนขวา | เทียบด้วยค่า dBm ดิบซึ่งแกว่งเกินสามหน่วยแทบทุกวินาที แถบบนจึงถูกล้างแล้ววาดใหม่ตลอด ตอนนี้เทียบด้วยจำนวนขีดแทน |

### ตัวทดสอบจับได้อีกหนึ่งบั๊ก

**แถบความคืบหน้าเป็นสี่เหลี่ยมมุมโค้ง `fillRoundRect` จึงไม่ลงสีที่มุมทั้งสี่**
เวลายอดลดลง สีเขียวของค่าเก่าจะค้างอยู่ที่มุม แก้ด้วยการล้างกรอบสี่เหลี่ยมเต็มก่อน
หนึ่งครั้ง ซึ่งทำได้เพราะแถบนี้วาดเฉพาะตอนยอดเปลี่ยน ไม่ได้วาดทุกวินาที

### ตัวทดสอบเข้มขึ้น

`run_partial.mjs` เพิ่มการ **นับจำนวนครั้งที่ล้างพื้นระหว่างวาดซ้ำ**
กรณีที่วาดทุกวินาที (นาฬิกาทั้งสามหน้า) ต้องได้ศูนย์ครั้ง ไม่งั้นถือว่าสอบตก
เพราะถ้ายังมีการล้างพื้นอยู่ ก็คือยังกะพริบอยู่

ผ่านครบ 18 กรณี ทั้งเรื่องพิกเซลตรงกันและเรื่องไม่ล้างพื้น

### แก้ตัวจำลองให้ตรงกับจอจริงด้วย

ตัวจำลองใช้ฟอนต์ของระบบซึ่งมีหางตัวอักษรล้นออกนอกเซลล์ได้ ต่างจากฟอนต์ในตัว
ของ Adafruit GFX ที่อยู่ในกรอบเซลล์เสมอ ทำให้ตัวทดสอบรายงานความต่างปลอม ๆ
สองพิกเซล ตอนนี้ตัดขอบตัวอักษรให้เท่าเซลล์แล้ว

## ซ่อมการรีเฟรชตัวเลขบนจอทั้งสองฝั่ง (Host 113.2.0 / Station 122.2.0)

ท่านให้ตรวจว่าจอรีเฟรชเมื่อตัวเลขเปลี่ยนหรือไม่ ตรวจแล้ว **พบว่าหลายจุดไม่รีเฟรช**
บางจุดเป็นของเดิม บางจุดผมทำพังเองตอนออกแบบหน้าจอใหม่ สรุปตามตาราง

| จุด | อาการเดิม | ใครทำ |
|---|---|---|
| จอแม่ข่าย หน้า **SHOPS** | ยอดรายร้านวาดเฉพาะตอนวาดใหม่ทั้งจอ มีคนมารับอาหารแล้วตัวเลขไม่ขยับ | ผมเอง ตอนออกแบบใหม่ |
| จอแม่ข่าย หน้า **SYSTEM** | ไม่เคยรีเฟรชเลย เพราะ loop() รีเฟรชแค่หน้า 1 กับ 2 อุณหภูมิและหน่วยความจำจึงค้าง | ของเดิม แต่ผมย้ายค่าที่ต้องขยับมาไว้หน้านี้พอดี |
| จอแม่ข่าย หน้าพักจอ | ยอดวันนี้วาดครั้งเดียวตอนเข้าโหมด | ของเดิม |
| จอจุดบริการ หน้า 2 | โค้ดอัปเดตยอดใน loop() ใช้พิกัดและขนาดตัวอักษรของหน้าจอชุดเก่า วาดผิดที่และทับของเดิมไม่มิด | ผมเอง ตอนออกแบบใหม่ |
| จอจุดบริการ หน้า 1 และ 2 | นาฬิกาค้าง ไม่เดิน และสถานะออนไลน์/ออฟไลน์ไม่ขยับจนกว่าจะกดเปลี่ยนหน้า | ของเดิม |
| จอจุดบริการ แถบสัญญาณมุมขวาบน | `updateTopRightHeaderSmooth()` เขียนไว้ครบแต่**ไม่เคยถูกเรียกเลย** แถบสัญญาณจึงไม่เคยขึ้น | ของเดิมตั้งแต่ 117.0.7 |

### วิธีแก้

ทุกจอเปลี่ยนมาใช้หลักเดียวกัน คือ **เทียบค่ากับที่วาดไว้ครั้งก่อน แล้ววาดซ้ำเฉพาะช่องที่เปลี่ยนจริง**
ไม่ล้างแล้ววาดใหม่ทั้งการ์ดทุกวินาที จอจึงไม่กะพริบและตัวเลขก็ไม่ค้าง

- ฝั่งแม่ข่าย `renderHostPage()` เก็บค่าที่วาดไว้ครั้งก่อนของทุกช่อง และ loop()
  รีเฟรช **ทุกหน้า** ทุกหนึ่งวินาที ไม่ใช่แค่สองหน้าแรก
- ฝั่งจุดบริการ เพิ่ม `refreshStationLiveValues()` ไว้ใน `StationScreen.h`
  **อยู่ข้างเดียวกับโค้ดที่วาดหน้านั้นจริง** พิกัดจึงแก้พร้อมกันเสมอ
  ต้นเหตุของบั๊กเดิมคือพิกัดถูกเขียนไว้สองที่แล้วหลุดจากกัน
- แถบสัญญาณถูกต่อสายเข้ากับรอบรีเฟรชแล้ว และมีการ์ดกันไม่ให้ไปวาดทับ
  หน้าแจ้งผลการแตะบัตรซึ่งยึดพื้นที่ทั้งจอ

### เพิ่มตัวทดสอบว่าการวาดบางส่วนไม่ทิ้งเศษพิกเซล

การวาดเฉพาะบางช่องมีกับดักคือ **ถ้าช่องที่ล้างเล็กกว่าของเดิม จะเหลือเศษค้างบนจอ**
เช่น 187 เปลี่ยนเป็น 9 แล้วเลข 87 ยังอยู่

`tools/screen-preview/run_partial.mjs` ทดสอบให้อัตโนมัติ โดยเทียบภาพที่ได้จาก
การวาดใหม่ทั้งหน้า กับการวาดค่าเก่าแล้วแก้เฉพาะช่อง **สองภาพต้องตรงกันทุกพิกเซล**
ทดสอบ 9 กรณี คูณสองโหมดสี รวม 18 กรณี

ตัวทดสอบจับบั๊กจริงได้ทันทีหนึ่งตัว: เปลี่ยนป้าย ONLINE เป็น OFFLINE แล้ว
**สีเขียวค้างอยู่ที่มุมโค้งทั้งสี่ของป้าย** เพราะ `fillRoundRect` ไม่ได้ลงสีที่มุม
การวาดป้ายใหม่ทับจึงไม่ลบมุมเดิม ต้องล้างกรอบสี่เหลี่ยมเต็มก่อน
แก้แล้วและตอนนี้ผ่านครบทั้ง 18 กรณี

### ข้อจำกัดที่ต้องบอกตรง ๆ

ตัวจำลองใน `tools/screen-preview/` เป็น**สำเนาที่เขียนตามโค้ดเฟิร์มแวร์ ไม่ใช่ตัวเฟิร์มแวร์เอง**
พิสูจน์ได้แค่เรื่องเรขาคณิตของการวาด ไม่ได้พิสูจน์ว่าโค้ดบนบอร์ดจริงทำงานถูก
**ยังต้องรบกวนท่านทดสอบบนเครื่องจริง** โดยเฉพาะข้อที่ว่านาฬิกาเดินจริงไหม
และยอดขยับทันทีเมื่อมีคนแตะบัตร

## วางมาตรฐานคอมเมนต์และบังคับใช้ด้วยตัวตรวจ (เอกสารและคอมเมนต์ ไม่แตะตรรกะ)

ท่านขอให้คอมเมนต์เป็นมาตรฐานสากล อ้างอิงเลขเวอร์ชันได้ และบอกได้ว่า
โค้ดชุดหนึ่งเพิ่มอะไรเข้าไปบ้าง จึงวางไว้เป็นสองชั้น

### ชั้นที่หนึ่ง — หัวไฟล์แบบ Doxygen

ทุกไฟล์ `.ino` และ `.h` ขึ้นต้นด้วยบล็อกเดียวกัน มี `@file` `@brief` `@version`
`@date` `@author` พร้อม **ตาราง Revision History** ที่บอกว่าแต่ละรุ่นเปลี่ยนอะไร
ในไฟล์นั้น และ `@warning` สำหรับเรื่องที่ไม่รู้แล้วจะพัง เช่นเรื่องบัส SPI ของจอ
กับเครื่องอ่านบัตร หรือเรื่อง Partition Scheme ที่เลือกผิดแล้วฐานข้อมูลหาย

ใช้แท็กจริงของ Doxygen ทั้งหมด ไม่ได้คิดขึ้นเอง ถ้าวันหนึ่งอยากสร้างเอกสาร
อัตโนมัติจากโค้ด ก็รัน Doxygen ได้เลยโดยไม่ต้องแก้อะไร

หัวไฟล์เดิมของทั้งสองสเก็ตช์ยังเขียนว่าเวอร์ชัน 107.0.1 กับ 117.0.7 อยู่
ทั้งที่ `APP_VERSION` เดินไปถึง 113.1.0 กับ 122.1.0 แล้ว ซึ่งเป็นปัญหาที่
มาตรฐานนี้ตั้งใจแก้พอดี

### ชั้นที่สอง — ป้ายบอกเวอร์ชันในเนื้อโค้ด

หัวไฟล์บอกภาพรวม แต่ไม่ได้บอกว่าบรรทัดไหนมาจากรุ่นไหน จึงปักป้ายไว้เหนือจุดที่
เพิ่มหรือแก้จริง รูปแบบ `// [113.0.0] เพิ่ม: คำอธิบาย` โดยคำกริยาใช้ได้สามคำ
คือ เพิ่ม / แก้ / ย้าย

ปักไว้ 19 จุด เฉพาะที่ยืนยันได้จากประวัติการแก้ไขจริง **ไม่ได้เดา** และไม่ปักกับ
การแก้เล็ก ๆ ที่อ่านโค้ดแล้วเข้าใจได้เอง เพราะป้ายที่เยอะเกินไปจะกลายเป็นขยะ

### บังคับใช้ด้วย `tools/proto-check/headercheck.py`

ตรวจห้าข้อ: แท็กบังคับครบ · `@file` ตรงชื่อไฟล์จริง · `@version` สอดคล้องกับ
`APP_VERSION` · `@date` เป็นรูปแบบ `YYYY-MM-DD` · และป้ายในโค้ดอ้างเลขที่มีอยู่จริง
ในตารางของไฟล์นั้น พร้อมใช้คำกริยาตามมาตรฐาน

ทดสอบด้วยการใส่บั๊กห้าแบบกลับเข้าไปในไฟล์สำเนา ตัวตรวจชี้ได้ครบทั้งห้า

**เรื่องที่ตัวตรวจสอนผมระหว่างทาง** รอบแรกผมตั้งกติกาว่า `@version` ของทุกไฟล์
ต้องเท่ากับ `APP_VERSION` ตัวตรวจจึงฟ้อง `HostScreen.h` ทันที เพราะรุ่น 113.1.0
แก้แค่หน้าเว็บ ไม่ได้แตะไฟล์วาดจอเลย ถ้าไล่ขยับเลขให้เท่ากันหมดตารางจะโกหกว่า
ไฟล์นั้นถูกแก้ทั้งที่ไม่ได้แก้ จึงเปลี่ยนกติกาเป็น `@version` ของไฟล์ `.h` คือ
**เวอร์ชันที่ไฟล์นั้นถูกแก้ครั้งล่าสุด** และต้องไม่ใหม่กว่า `APP_VERSION`

รายละเอียดทั้งหมดอยู่ใน [`docs/CODE-STANDARD.md`](docs/CODE-STANDARD.md)

## แยกส่วนวาดจอของฝั่งจุดบริการออกมา (Station 122.1.0)

ทำแบบเดียวกับฝั่งแม่ข่าย ย้ายฟังก์ชันที่วาดลงจอ TFT ทั้ง 25 ตัวไปไว้ที่
`StationScreen.h` **ย้ายแบบยกก้อนทั้งดุ้น ไม่แก้เนื้อในแม้แต่ตัวอักษรเดียว**
หน้าจอจึงแสดงผลเหมือนเดิมทุกพิกเซล

| ไฟล์ | ก่อน | หลัง |
|---|---|---|
| `Canteen_Station_Client.ino` | 1,775 บรรทัด | **1,141 บรรทัด** |
| `StationScreen.h` | — | 707 บรรทัด |

สิ่งที่ย้ายไป: ชิ้นส่วนพื้นฐาน (การ์ด ป้าย แถบบน แถบล่าง การย่อตัวอักษรให้พอดีกรอบ),
แถบสัญญาณมุมขวาบน, การเปิด-ปิดไฟส่องหลังจอ และหน้าจอเต็มทั้งหมด
สิ่งที่อยู่ในไฟล์หลักเหมือนเดิม: ESP-NOW, การอ่านบัตร, ปุ่มกด, เสียง, ไฟ LED,
การตั้งหมายเลขจุดบริการ และการรับคำสั่งโหมดการแสดงผลจากแม่ข่าย

ตรวจว่าไม่มีอะไรหล่นหายด้วยการนับบรรทัดโค้ดทุกบรรทัดก่อนและหลังย้าย
ผลคือเท่ากันเป๊ะ ไม่มีบรรทัดไหนหายและไม่มีบรรทัดไหนงอกขึ้นมา

### เจอจุดที่จะคอมไพล์ไม่ผ่าน และแก้แล้วสองจุด

1. `drawStationIdConfigProgress()` ถูกเรียกจากไฟล์หลักแต่ไม่มีการประกาศล่วงหน้า
   พอนิยามย้ายไปอยู่ในไฟล์ที่ `#include` ทีหลัง จะขึ้น error แบบเดียวกับที่เคยเจอ
2. `updateTopRightHeaderSmooth()` มีค่าปริยาย `= false` อยู่ที่ตัวนิยาม
   ซึ่งเดิมอยู่ก่อนจุดที่มีคนเรียก พอย้ายไปอยู่ทีหลังจะพังทันที
   ย้ายค่าปริยายไปไว้ที่การประกาศล่วงหน้าแทน

### เพิ่มเครื่องมือ `tools/proto-check/splitcheck.py`

ตรวจปัญหาสองแบบข้างบนให้อัตโนมัติ สำหรับทุกไฟล์ `.h` ที่สเก็ตช์ `#include` ไว้
ทดสอบแล้วด้วยการใส่บั๊กทั้งสองแบบกลับเข้าไปในไฟล์สำเนา ตัวตรวจชี้ได้ถูกทั้งคู่

ตัวตรวจลอกสตริงและคอมเมนต์ออกก่อนเสมอ โค้ด JavaScript ที่ฝังอยู่ใน `WebDashboard.h`
จึงไม่ถูกเข้าใจผิดว่าเป็นฟังก์ชันของ C++ และฟังก์ชันที่ใช้อยู่ภายในไฟล์ `.h` เองเท่านั้น
ก็ไม่ถูกเตือน เพราะไม่จำเป็นต้องประกาศล่วงหน้า

## หน้าเว็บกลับมาเป็นสองภาษา ไทย/อังกฤษ (Host 113.1.0)

ตามที่ท่านขอให้เอาระบบสองภาษากลับมา ทำเฉพาะฝั่งหน้าเว็บ ฝั่งจอ TFT ยังเป็นอังกฤษ
เพราะฟอนต์ในตัวไลบรารี Adafruit GFX ไม่มีตัวอักษรไทย

**วิธีทำต่างจากของเดิม** เฟิร์มแวร์รุ่นเก่าเขียนข้อความซ้ำสองชุดในทุกบรรทัด
ด้วยแอตทริบิวต์ `data-th` กับ `data-en` ซึ่งทำให้หน้าเว็บโตและแก้ยาก
รุ่นนี้เขียนข้อความอังกฤษไว้ใน HTML ตามปกติแล้วติดป้าย `data-i="คีย์"`
ส่วนคำไทยอยู่ในพจนานุกรมชุดเดียวที่หัวของ `DASH_JS` เพิ่มข้อความใหม่ทีหลัง
จึงแก้แค่สองที่ และถ้าลืมเติมคำแปล หน้าเว็บจะแสดงอังกฤษไว้ก่อน ไม่พัง

- แปลครบ 63 จุด ทั้งแท็บ หัวตาราง ปุ่มในแถว ป้ายสถานะ ตัวเลือกในดรอปดาวน์
  ข้อความช่วยเหลือ ข้อความแจ้งผล และกล่องถามยืนยันก่อนลบ
- หน้าเข้าสู่ระบบสลับได้ด้วย มีคำแปลชุดย่อของตัวเองห้าคำ ไม่ต้องโหลด `/a.js`
- ค่าเริ่มต้นเป็นอังกฤษ และเบราว์เซอร์จำค่าที่เลือกไว้
- `/a.js` ส่งพร้อม `charset=utf-8` แล้ว เพราะในไฟล์มีคำแปลภาษาไทย
- กินแฟลชเพิ่มราว 8 KB (สคริปต์โตจาก 8.5 KB เป็น 16.6 KB)

### เพิ่มตัวตรวจคำพูดไม่ครบคู่

ระหว่างทำงานนี้ผมแก้ข้อความ HTML ที่ฝังอยู่ในสตริง C++ แล้ว**ลบเครื่องหมายคำพูด
เปิดทิ้งไปสองบรรทัด** ซึ่งคอมไพล์ไม่ผ่านแน่นอน แต่ตัวนับปีกกาเดิมมองไม่เห็น
เพราะปีกกายังครบทุกตัว จับได้ตอนเรนเดอร์ทดสอบเท่านั้น

`tools/proto-check/bracecheck.py` จึงเพิ่มการตรวจว่ามีสตริงไหนเปิดแล้วไม่ปิด
ภายในบรรทัดเดียวกันหรือไม่ ทดสอบแล้วด้วยการใส่บั๊กเดิมกลับเข้าไปในไฟล์สำเนา
ตัวตรวจชี้บรรทัดที่ผิดได้ถูกต้อง

## เริ่มใหม่จากต้นฉบับ แล้วรื้ออินเตอร์เฟสทั้งหมด (Host 113.0.0 / Station 122.0.0)

รุ่นนี้ **ไม่ได้ต่อยอดจากสายเดิม** แต่กลับไปเริ่มจากไฟล์ต้นฉบับ 107.0.1 และ 117.0.7
ตามที่ท่านเลือก แล้วค่อย ๆ ใส่ของที่จำเป็นกลับเข้าไปทีละอย่าง เพื่อให้แน่ใจว่า
ไม่มีของที่ผมเติมเข้าไปโดยไม่ได้ทดสอบหลงเหลืออยู่

### หนึ่ง — แยกไฟล์ให้ไฟล์หลักสั้นลง

ไฟล์เดียวสามพันบรรทัดทำให้คนที่มารับช่วงดูแลต่อหาของไม่เจอ ตอนนี้แยกเป็น

| ไฟล์ | เนื้อหา |
|---|---|
| `Canteen_Host_Server.ino` | ตรรกะระบบ ฐานข้อมูล ESP-NOW เส้นทางเว็บ |
| `WebDashboard.h` | หน้าเว็บทั้งหมด |
| `HostScreen.h` | ทุกอย่างที่วาดลงจอ TFT |

ทั้งสองไฟล์ถูก `#include` ไว้ท้ายไฟล์หลักก่อน `setup()` **ห้ามย้ายขึ้นไปบนสุด**
เพราะโค้ดข้างในอ้างถึงตัวแปรส่วนกลางที่ประกาศไว้ข้างบน

### สอง — อินเตอร์เฟสใหม่ ภาษาอังกฤษทั้งหมด สองโหมด

ทั้งเว็บและจอ TFT ถูกออกแบบใหม่ตามที่ท่านสั่ง: เรียบง่าย แบ่งช่องชัดเจน
ตัวอักษรน้อย และ **เป็นภาษาอังกฤษทั้งหมด** เพราะนิสิตส่วนใหญ่เป็นชาวต่างชาติ
ส่วนคำอธิบายในโค้ดยังเป็นภาษาไทยไว้ให้คนดูแลระบบอ่าน

หน้าเว็บมีสี่แท็บ Today / Students / Shops / Settings แทนเมนูซ้อนแบบเดิม
หน้าแรกคือสิ่งที่เจ้าของร้านอยากรู้: ขายได้กี่จาน เป็นเงินเท่าไร เทียบกับร้านอื่น
ข้อมูลเชิงเทคนิคอย่างอุณหภูมิชิปกับโหลดซีพียูย้ายไปอยู่หน้า SYSTEM ของจอ TFT

จอแม่ข่ายเหลือสามหน้า TODAY / SHOPS / SYSTEM
จอจุดบริการหน้าแรกมีประโยคเดียวที่นิสิตต้องอ่านคือ TAP YOUR CARD

### สาม — เครื่องแม่ข่ายเป็นผู้กำหนดการแสดงผลของทุกจุดบริการ

โหมดมืด/สว่าง และการพักหน้าจอ ถูกสั่งจากแม่ข่ายที่เดียว ผ่านหน้าเว็บหรือปุ่มบนเครื่อง
ปุ่มสลับธีมที่ตัวจุดบริการถูกถอดออก เพราะถ้าแต่ละจุดตั้งเองได้ จอในโรงอาหาร
จะไม่เหมือนกันและไม่มีใครรู้ว่าใครไปกดอะไรไว้

### สี่ — ประสิทธิภาพ

- CSS กับ JavaScript ของหน้าเว็บย้ายไปอยู่ในแฟลช (`PROGMEM`) เสิร์ฟแยกที่
  `/s.css` และ `/a.js` ไม่ต้องต่อสตริงสองหมื่นตัวอักษรในแรมทุกครั้งที่เปิดหน้า
  และเบราว์เซอร์แคชไว้ได้ หน้าเว็บจึงเปิดเร็วขึ้นมากตั้งแต่ครั้งที่สองเป็นต้นไป
- ตัวเลขสดบนหน้าเว็บมาจาก `/api/dashboard` ทุกสองวินาที ไม่ต้องรีโหลดทั้งหน้า
- รายชื่อนิสิตเลิกใช้ `DynamicJsonDocument` ขนาดคงที่ 8 กิโล ซึ่งพอดีเกินไป
  สำหรับยี่สิบรายชื่อ ถ้าล้นขึ้นมา JSON จะขาดกลางคันแบบเงียบ ๆ ไม่มีใครรู้

### ห้า — ของเดิมที่ผิดอยู่ และแก้ในรุ่นนี้

| อาการ | สาเหตุ |
|---|---|
| เลขอ้างอิงบนจอไม่ตรงกับที่บันทึกไว้ | สร้างเลขใหม่ทุกครั้งที่วาดจอ ตัวนับจึงเดินเรื่อย ๆ |
| หน้าตรวจสอบระบบฝั่งสถานี มีหัวข้อแต่ไม่มีค่า | แถวแบตเตอรี่กับยอดวันนี้ไม่เคยถูกวาดค่าลงไป |
| ชื่อร้านบนจอแม่ข่ายหายไปครึ่งหนึ่ง | ช่องล้างพื้นที่ของจุดสถานะวาดทับตัวอักษร |
| ไฟล์ CSV คอลัมน์เลื่อนทั้งแถว | ชื่อที่มีเครื่องหมายคำพูดไม่ถูกครอบให้ถูกแบบ |
| โทเคนเข้าสู่ระบบเดาได้ | `random()` ของ Arduino ให้ลำดับเดิมทุกครั้งที่เปิดเครื่อง เปลี่ยนไปใช้ตัวสุ่มในตัวชิป |
| ลิงก์เดียวลบข้อมูลได้ | เส้นทางที่เปลี่ยนแปลงข้อมูลเปลี่ยนจาก GET เป็น POST ทั้งหมด |
| ดาวน์โหลดไฟล์ไหนก็ได้ในเครื่อง | ชื่อไฟล์แบบ `../` ผ่านเข้าไปได้ ตอนนี้รับเฉพาะ `arc_*` ในรากเท่านั้น |

### ข้อจำกัดที่ต้องบอกตรง ๆ

ผมยังคอมไพล์จริงไม่ได้ในที่นี้ (`downloads.arduino.cc` ถูกบล็อกที่พร็อกซี)
สิ่งที่ผมทำได้คือตรวจวงเล็บ ตรวจการประกาศฟังก์ชันล่วงหน้า ตรวจไวยากรณ์ JavaScript
ที่ฝังอยู่จริง และเรนเดอร์ทุกหน้าจอด้วยตัวจำลองเพื่อดูว่าตัวอักษรไม่ล้นกรอบ
**การคอมไพล์และทดสอบบนเครื่องจริงต้องรบกวนท่านเป็นคนทำ**

## แก้ปัญหาชื่อ Wi-Fi ไม่โผล่มา (Host 112.0.0 / Station 121.0.0)

อาการ: แฟลชเสร็จแล้วแต่ไม่มีชื่อ `MCU_CANTEEN_HOST` ขึ้นมาให้เลือกบนโทรศัพท์
ไล่ดูลำดับการเปิด Wi-Fi แล้วพบสาเหตุที่เป็นไปได้สองข้อ **ซึ่งเป็นความผิดพลาดของผมเองทั้งคู่**

**1. `WIFI_PROTOCOL_LR` บน SoftAP**

ตอนทำเรื่องความทนทานของลิงก์ (รุ่น `release/05`) ผมใส่ `WIFI_PROTOCOL_LR` ลงไปใน
bitmap ของ SoftAP พร้อมเขียนคอมเมนต์การันตีไว้ว่า *"การใส่เพิ่มเฉย ๆ ปลอดภัยเพราะยังคง
11b/g/n ไว้ โทรศัพท์และคอมพิวเตอร์จึงต่อ Wi-Fi ได้ตามปกติ"* — **ผมไม่เคยทดสอบข้อนี้เลย**
ความจริงคือ LR เป็นโหมดเฉพาะของ Espressif และทำให้อุปกรณ์ทั่วไปหลายรุ่น
**มองไม่เห็นชื่อ Wi-Fi** ทั้งที่เครื่องปล่อยสัญญาณอยู่

ตอนนี้ใส่สวิตช์ `ENABLE_WIFI_LONG_RANGE` ไว้ทั้งสองฝั่ง **ค่าเริ่มต้นคือปิด**
ประโยชน์จริงของ LR มาจากการบังคับอัตราส่ง (`ESPNOW_FORCE_LONG_RANGE_RATE`)
ซึ่งปิดอยู่แล้ว การใส่ LR ลง bitmap เฉย ๆ จึงแทบไม่ช่วยอะไรแต่เสี่ยงมาก

**2. `printerBegin()` ถูกเรียกก่อนเปิด Wi-Fi**

ผมเขียนคอมเมนต์ไว้ว่า *"เริ่มสแต็กเครื่องพิมพ์ก่อนเปิด Wi-Fi เผื่อฝั่ง USB host
ต้องใช้เวลาจับอุปกรณ์"* ซึ่งเป็นเหตุผลที่ผิด ไม่มีอะไรในเครื่องพิมพ์ที่ต้องมาก่อน Wi-Fi
แต่ถ้า `usb_host_install()` ค้างหรือแพนิก (เกิดง่ายมากเมื่อยังไม่ได้จ่ายไฟ 5V เข้าขา VBUS)
จะไม่มีชื่อ Wi-Fi โผล่มาเลยและเข้าไปแก้อะไรไม่ได้ทั้งสิ้น
ย้ายไปเรียก **หลัง** `server.begin()` แล้ว

**สิ่งที่เพิ่มเพื่อให้วินิจฉัยได้เองในสนาม**

- ลองเรียก `WiFi.softAP()` ซ้ำได้ถึงสามครั้ง และเก็บผลไว้ในตัวแปร `apStarted`
- **จอแจ้งสถานะเครือข่ายตอนบูต** แสดงชื่อ Wi-Fi รหัสผ่าน หมายเลขเครื่อง ช่องสัญญาณ
  และเลขเวอร์ชันคาตา ถ้าเปิด AP ไม่สำเร็จจะขึ้นแถบแดงพร้อมเสียงเตือนต่ำสองครั้ง
  เครื่องนี้ไม่ได้เปิด Serial ไว้ จอจึงเป็นช่องทางเดียวที่บอกได้
- เพิ่มหัวข้อ **แก้ปัญหาที่พบบ่อย** ใน README

## แยกไฟล์หลักให้ไม่เกิน 3,000 บรรทัด (Host 111.1.0)

ไฟล์หลักโตขึ้นเป็น 3,502 บรรทัด เกินเกณฑ์ที่โครงการนี้ถือปฏิบัติไว้ว่าไม่ควรเกิน 3,000
แยกออกเป็นสองไฟล์ใหม่ เหลือไฟล์หลัก **2,674 บรรทัด**

| ไฟล์ใหม่ | เนื้อหา | บรรทัด |
|---|---|---|
| `HostDisplay.h` | ทุกอย่างที่วาดลงจอ TFT ตั้งแต่ชิ้นส่วนการ์ด/แถบ ไปจนถึงสามหน้าหลัก โหมดพักจอ ผลการสแกน และหน้าเครดิต | 608 |
| `HostSlips.h` | เนื้อหาสลิปเครื่องพิมพ์และปลายทาง API ทั้งสี่ (ทั้งก้อนอยู่ใต้ `#if ENABLE_THERMAL_PRINTER` ตามเดิม) | 270 |

**เป็นการย้ายที่อยู่ล้วน ๆ ไม่ได้แก้ตรรกะแม้แต่บรรทัดเดียว** ตรวจยืนยันด้วยการเทียบ
เนื้อในของฟังก์ชันทั้ง 107 ตัวกับคอมมิตก่อนหน้าแบบไบต์ต่อไบต์ — ไม่มีตัวไหนหายและ
ไม่มีตัวไหนเนื้อในเปลี่ยน ตรวจเพิ่มว่าไม่มีฟังก์ชันที่ย้ายออกไปถูกเรียกก่อนจุด `#include`
โดยไม่มี forward declaration

> **หมายเหตุสำคัญ** การแยกไฟล์ **ไม่ได้ทำให้เฟิร์มแวร์เล็กลงเลย** โค้ดปริมาณเท่าเดิม
> คอมไพล์ออกมาได้จำนวนไบต์เท่าเดิมเป๊ะ ทำเพื่อให้อ่านง่ายและดูแลต่อได้เท่านั้น
> เรื่อง `Sketch too big` ยังต้องแก้ด้วยการตั้ง Partition Scheme เป็น
> *Huge APP (3MB No OTA/1MB SPIFFS)* ตามที่ระบุไว้ใน README

## ปิดเครื่องพิมพ์สลิปเป็นค่าเริ่มต้น เพื่อลดขนาดเฟิร์มแวร์ (Host 111.0.0)

ตามที่ตกลงกันว่า **ฟีเจอร์เดิมคงไว้ทั้งหมด ถ้าจะตัดให้ตัดเครื่องพิมพ์สลิปก่อน**
เพราะเป็นของที่เพิ่งเพิ่มเข้ามาในรุ่น `release/06` ไม่ใช่ของเดิมของระบบ

- `#define ENABLE_THERMAL_PRINTER` เปลี่ยนค่าเริ่มต้นจาก `1` เป็น `0`
- ครอบส่วนสร้างเนื้อหาสลิปทั้งก้อน (`asciiSafe`, `asciiName`, `slipClip`, `slipHeader`,
  `slipFooter`, `buildClaimSlip`, `buildDailySlip`, `buildTestSlip`, `printClaimSlip`
  และปลายทาง API ทั้งสี่) ด้วย `#if ENABLE_THERMAL_PRINTER` เหลือเพียงโครงเปล่า
  ตอนปิด จึงไม่มีทั้งสแต็ก USB host และข้อความสลิปถูกคอมไพล์เข้าไปเลย
  ของเดิมปิดแค่ไดรเวอร์ แต่เนื้อหาสลิปยังถูกคอมไพล์ทิ้งไว้เปล่า ๆ
- ตัด `printerGuard()` ทิ้ง ไม่จำเป็นอีกแล้วเมื่อทั้งก้อนอยู่ใต้ `#if`
- หน้าเว็บลดระดับลงอย่างสุภาพ: ปุ่มพิมพ์ใบสรุปบนแดชบอร์ด **ไม่ถูกส่งออกมาเลย**
  (ประหยัดไบต์ด้วย) แท็บตั้งค่ามีแถบเตือนสีเหลืองบอกว่าปิดไว้ในเฟิร์มแวร์
  พร้อมวิธีเปิดกลับ และป้ายบนแดชบอร์ดขึ้นว่า *ปิดการใช้งานในเฟิร์มแวร์*

**ฟีเจอร์อื่นไม่ถูกแตะเลย** — จอสาธารณะ `/display` สองภาษา `ArduinoJson`
`ESPmDNS` คิวออฟไลน์ และการตรวจสิทธิ์ตอนลิงก์ขาด ยังอยู่ครบตามเดิม

เมื่อต่อเครื่องพิมพ์แล้วให้แก้ `#define ENABLE_THERMAL_PRINTER` เป็น `1`
แล้วอัปโหลดใหม่ ฟีเจอร์กลับมาครบทันที

> ยังวัดไม่ได้ว่าประหยัดไปกี่ไบต์จริง เพราะเครื่องที่ใช้พัฒนาคอมไพล์ไม่ได้
> ถ้าคอมไพล์แล้วช่วยส่งบรรทัด `Sketch uses ...` กลับมา จะได้เอาตัวเลขจริง
> ไปใส่ใน README แทนค่าประมาณ

## แก้คำแนะนำการตั้งพาร์ทิชันที่ผิด (เอกสาร)

คอมไพล์แล้วขึ้น `Sketch too big` ใช้ไป 1,339,228 ไบต์ จากเพดาน 1,310,720 ไบต์

**สาเหตุไม่ใช่โค้ดยาวเกินไป แต่เป็นการตั้งพาร์ทิชันผิด** เลข 1,310,720 คือขนาด
พาร์ทิชันโปรแกรมของ *Default 4MB with spiffs* ทั้งที่บอร์ด N16R8 มีแฟลช 16 MB
พอเปลี่ยนเป็น *Huge APP (3MB No OTA/1MB SPIFFS)* จะใช้ไปเพียง 43%

**คำแนะนำเดิมใน README ผิดและอันตรายกว่านั้น** — เขียนไว้ว่าให้ใช้
*16M Flash (3MB APP/9.9MB FATFS)* ซึ่งแม้พื้นที่โปรแกรมจะพอ แต่พาร์ทิชันไฟล์
ถูกตั้งชื่อว่า `ffat` ขณะที่โค้ดเรียก `LittleFS.begin(true)` ซึ่งมองหาชื่อ `spiffs`
ผลคือเมานต์ไม่ขึ้นและ **ฐานข้อมูลนิสิตทั้งหมดหายไป** แก้เป็นตารางการตั้งค่าบอร์ด
แบบครบถ้วนพร้อมคำเตือน

เพิ่มหัวข้อ **ขนาดเฟิร์มแวร์** ใน README ระบุว่าส่วนไหนกินแฟลชเท่าไร
(หน้าเว็บที่ฝังอยู่ 106 KB เป็นก้อนใหญ่สุด) พร้อมตารางทางเลือกลดขนาด
เรียงตามความคุ้มค่าและสิ่งที่ต้องแลก เผื่อกรณีถูกบังคับให้ใช้พาร์ทิชันเล็ก

ไม่มีการแก้โค้ด เลขเวอร์ชันจึงเท่าเดิม

## แก้คอมไพล์ไม่ผ่านใน Arduino IDE (Host 110.0.2 / Station 120.0.1)

คอมไพล์จริงบน Arduino IDE แล้วไม่ผ่าน

```
error: variable or field 'fillStationConfig' declared void
error: 'HostConfigPacket' was not declared in this scope
```

สาเหตุคือ Arduino IDE **สร้างต้นแบบฟังก์ชันให้เองอัตโนมัติ** แล้ววางไว้ใกล้หัวไฟล์
ซึ่งอยู่ **เหนือจุดที่นิยาม struct** ฟังก์ชันที่รับ struct ของสเก็ตช์เป็นพารามิเตอร์
จึงต้องมีการประกาศล่วงหน้าไว้เองเสมอ พอมีแล้ว Arduino จะไม่สร้างต้นแบบซ้อนให้อีก

ไล่ตรวจทั้งสองไฟล์แล้วพบสองจุด แก้ทั้งคู่

- `fillStationConfig(HostConfigPacket&, uint8_t)` ฝั่งแม่ข่าย — จุดที่ทำให้คอมไพล์
  ไม่ผ่าน มีมาตั้งแต่รุ่น `release/03` ตอนเพิ่มการซิงค์โหมดการแสดงผล
- `rosterApplyPacket(const HostRosterPacket&)` ฝั่งสถานี — เพิ่งเพิ่มมาใน
  `release/07` ยังไม่ทันได้เจอเพราะฝั่งแม่ข่ายพังก่อน

เพิ่ม [`tools/proto-check/`](tools/proto-check/) ไว้ตรวจปัญหาชนิดนี้โดยไม่ต้องมี
toolchain ของ ESP32 และเขียนเตือนไว้ใน README สำหรับการแก้โค้ดต่อ

> **หมายเหตุเรื่องคำเตือนตอนคอมไพล์** ถ้าขึ้นว่า `Multiple libraries were found for
> "Preferences.h"` แปลว่ามีโฟลเดอร์ `Preferences` ค้างอยู่ใน `~/Documents/Arduino/libraries/`
> ซ้ำกับที่มากับ ESP32 core ลบโฟลเดอร์นั้นทิ้งได้ ไม่ใช่สาเหตุของการคอมไพล์ไม่ผ่าน
> แต่ควรเอาออกกันสับสน

## อยู่ร่วมกับเฟิร์มแวร์สถานีรุ่นเก่าได้ (Host 110.0.1)

ตอนเขียนสารบัญเวอร์ชันแล้วไล่ตรวจว่าผสมรุ่นข้ามฝั่งได้จริงไหม พบว่ายังไม่ได้

เฟิร์มแวร์สถานีรุ่นก่อน 120.0.0 ไม่ได้ฝากเลขรุ่นบัญชีมากับ heartbeat (ช่องนั้นเป็นศูนย์ล้วน)
แม่ข่ายจึงอ่านได้เป็น "รุ่น 0" ซึ่งไม่มีวันตรงกับรุ่นของตัวเอง ผลคือ**ไล่ผลักบัญชีชุดเต็ม
ไปให้เครื่องที่รับไม่เป็นทุก heartbeat ไม่มีวันจบ** ประมาณ 11 แพ็กเก็ตทุก 6 วินาที

- แยกให้ออกจากสถานีรุ่นใหม่ที่ยังไม่มีบัญชี ซึ่งจะฝาก `R:0` มาเสมอ
  ใช้การมีอยู่ของคำนำหน้า `R:` เป็นตัวบอกว่าเฟิร์มแวร์รองรับหรือไม่
- ไม่ผลักบัญชีและไม่ส่งอัปเดตทีละรายการไปให้สถานีที่รับไม่เป็นเลย
- แดชบอร์ดขึ้นว่า *เฟิร์มแวร์เก่า ไม่รองรับ* ที่การ์ดจุดบริการนั้น
  จุดบริการยังทำงานครบทุกอย่าง แค่ไม่มีการตรวจสิทธิ์ตอนลิงก์ขาด
- ชุดทดสอบเพิ่มข้อ 10 ครอบคลุมทั้งกรณีรุ่นเก่าและตอนแฟลชเป็นรุ่นใหม่แล้ว

## จัดระเบียบเวอร์ชันและเก็บต้นฉบับ

- แบ่งเป็นรุ่น `release-00` ถึง `release-07` ทุกจุดที่เลขเวอร์ชันของฝั่งใดฝั่งหนึ่งเปลี่ยน
  แต่ละรุ่นคือเฟิร์มแวร์ครบทั้งคู่ที่ใช้ด้วยกันได้ ตรวจแล้วทีละรุ่นว่าชื่อรุ่นตรงกับ
  `APP_VERSION` ที่อยู่ในโค้ดของคอมมิตนั้นจริง
- จัดโครงสร้าง branch: ย้ายทุกรุ่นไปอยู่ใต้โฟลเดอร์ `release/` (`release/00-...`
  ถึง `release/07-...`) และเพิ่ม `main` เป็น branch หลักแทนชื่อสุ่มจากเซสชัน
  วิธีสลับ default branch กับคำสั่งลบของเก่าอยู่ใน `VERSIONS.md`
- push ขึ้น GitHub เป็น **branch** ไม่ใช่ tag เพราะบัญชีที่เซสชันนี้ใช้
  push `refs/tags/*` ไม่ได้
  เซิร์ฟเวอร์ตอบ 403 ทุกครั้ง (ยืนยันแล้วว่าไม่ใช่การปิดกั้นของพร็อกซี)
  ได้ผลเหมือน tag ทุกอย่างสำหรับการย้อนกลับ และกดโหลด zip ได้จากตารางใน `VERSIONS.md`
  ส่วนคำสั่งสร้าง tag จริงเตรียมไว้ให้คัดลอกไปรันครั้งเดียวจบเช่นกัน
- เพิ่ม [`VERSIONS.md`](VERSIONS.md) เป็นสารบัญเวอร์ชัน พร้อมวิธีย้อนกลับ
  และตารางความเข้ากันได้ของโปรโตคอล ซึ่งอ่านขนาดโครงสร้างจาก `static_assert`
  ในโค้ดของแต่ละแท็กจริง
- เพิ่มโฟลเดอร์ [`original/`](original/) เก็บโค้ดต้นฉบับที่ส่งมาตอนเริ่มโครงการ
  (Host 107.0.1 / Station 117.0.7) ไว้ตามเดิมทุกไบต์ ตรวจสอบด้วย md5 แล้วว่า
  ตรงกับไฟล์ที่ได้รับและตรงกับคอมมิตแรกของโครงการ วางไว้ในโฟลเดอร์ที่ชื่อ
  ตรงกับชื่อไฟล์ จึงเปิดด้วย Arduino IDE ได้ทันที

## ตรวจสิทธิ์ได้เองตอนลิงก์ขาด (Host 110.0.0 / Station 120.0.0)

ปิดช่องโหว่ที่เหลืออยู่ของคิวออฟไลน์: เดิมเมื่อแม่ข่ายไม่ตอบ สถานีรับบัตร **ทุกใบ**
เข้าคิวโดยไม่ตรวจอะไรเลย บัตรที่ไม่ได้ลงทะเบียนหรือบัตรที่ใช้สิทธิ์ไปแล้วก็ได้อาหารไปก่อน
แล้วค่อยไปตกตอนซิงค์ ซึ่งสายเกินกว่าจะเรียกคืนได้

**บัญชีสิทธิ์ย่อที่ผลักไปเก็บที่สถานี** — ข้อความชนิดใหม่ `MSG_ROSTER (5)`
และแพ็กเก็ต `HostRosterPacket` 206 ไบต์ (38 รายการต่อก้อน)

- เก็บเป็นค่าแฮช FNV-1a 32 บิตของเลขบัตร + สถานะใช้สิทธิ์ = 5 ไบต์ต่อคน
  นิสิต 600 คนใช้ 3 KB สถานีไม่เคยได้รับเลขบัตรจริงหรือชื่อนิสิตเลย
- ผลักชุดเต็มเมื่อชุดผู้มีสิทธิ์เปลี่ยน (ทุกเส้นทางผ่าน `saveDatabaseToFS()` จุดเดียว)
  และเมื่อสถานีรายงานเลขรุ่นไม่ตรง ทยอยส่งก้อนละ 30 ms ไม่แย่งจังหวะการสแกนสด
- ตอนมีคนใช้สิทธิ์ส่งแค่รายการเดียว (สถานีละ 1 แพ็กเก็ต) แทนการผลักทั้งชุดวันละ 268 รอบ
- สถานีฝากเลขรุ่นที่ถืออยู่ไปกับ heartbeat ผ่านช่อง `offlineTime` ที่ว่างอยู่แล้ว
  จึงไม่ต้องขยาย `StationPacket` และเฟิร์มแวร์สถานีรุ่นเก่าถูกมองเป็นรุ่น 0 โดยอัตโนมัติ
- เก็บลง NVS แบบหน่วงเวลา 5 นาที บัญชีจึงไม่หายเมื่อรีบูตกลางที่ลิงก์ขาด
  และไม่กินอายุแฟลชโดยไม่จำเป็น

**หลักการคือ "ไม่มั่นใจ ให้ปล่อยผ่าน"** เพราะการปฏิเสธนิสิตที่มีสิทธิ์จริงเสียหายกว่า
การปล่อยบัตรแปลกปลอมผ่านไปหนึ่งใบ ซึ่งแม่ข่ายปัดตกตอนซิงค์อยู่ดี
กรณีที่กลับไปรับบัตรทุกใบเหมือนเดิม: ยังไม่เคยได้รับบัญชี กำลังรับชุดใหม่
จำนวนผู้มีสิทธิ์เกิน `ROSTER_MAX` (600) หรือบัญชีค้างข้ามวัน

**สองจุดที่แก้ตั้งแต่ตอนออกแบบ เพราะจะพังของจริง**

- *บัญชีค้างข้ามวัน* — ถ้าเปิดเครื่องตอนเช้าก่อนแม่ข่ายขึ้นแล้วมีนิสิตมาแตะ บัญชีของเมื่อวาน
  จะทำให้ทุกคนที่กินเมื่อวานถูกปฏิเสธ แก้ด้วยการติดวันที่ (`rosterDate`) ไปกับทุกชุด
  ถ้าวันที่ไม่ตรงหรือนาฬิกายังไม่ซิงค์ จะไม่เชื่อสถานะ "ใช้สิทธิ์แล้ว" แต่ยังตรวจได้ว่า
  บัตรใบไหนไม่อยู่ในทะเบียน เพราะทะเบียนไม่เปลี่ยนรายวัน
- *บัญชีใหญ่เกินเก็บไหว* — เวอร์ชันแรกตั้งเลขรุ่นเป็น 0 เมื่อเก็บไม่ครบ ซึ่งทำให้แม่ข่าย
  ผลักชุดเต็มมาใหม่ทุก heartbeat ไม่จบ แก้เป็นรับเลขรุ่นไว้เสมอ แล้วกันด้วยธง
  `rosterTruncated` แทน พร้อมเติม `!` ต่อท้ายเลขรุ่นใน heartbeat ให้แดชบอร์ดขึ้นเตือน

**หน้าจอใหม่ฝั่งสถานี** — จอปฏิเสธสองแบบ (แดง = บัตรไม่อยู่ในทะเบียน, ส้ม = ใช้สิทธิ์ไปแล้ว)
ขึ้นชัดว่า `DO NOT SERVE THE STUDENT` เพื่อแยกจากจอฟ้าที่แปลว่าบันทึกไว้แล้วให้จ่ายได้เลย
และเพิ่มแถว `OFFLINE CARD LIST` บนหน้า 3/3

**แดชบอร์ด** — การ์ดจุดบริการแต่ละใบขึ้นสถานะ `📋 รายชื่อออฟไลน์`
(เขียว = พร้อมใช้พร้อมเลขรุ่น, เหลือง = กำลังส่งให้, แดง = เกินที่เครื่องเก็บไหว)

**การตรวจสอบ** — ดึงฟังก์ชันจริงจากทั้งสอง `.ino` มาคอมไพล์ด้วย g++ บนเครื่องพัฒนา
แล้วรันจำลองโปรโตคอลทั้งวงจร ผ่านครบ 30 ข้อ ครอบคลุมการผลักชุดเต็ม การอัปเดตทีละรายการ
delta หายกลางทางแล้วกู้คืนเอง ปิดยอดประจำวัน บัญชีเกินความจุ บัญชีค้างข้ามวัน
การชนกันของค่าแฮชในชุดนิสิต 412 คน เลขรุ่นวนรอบที่ 65535 และ heartbeat จากเฟิร์มแวร์รุ่นเก่า
สคริปต์ยังตรวจด้วยว่านิยามโครงสร้างแพ็กเก็ตสองฝั่งตรงกันทุกไบต์

> ยังไม่ได้คอมไพล์ด้วย toolchain ของ ESP32 (สภาพแวดล้อมที่พัฒนาเข้า `downloads.arduino.cc`
> ไม่ได้) และยังไม่ได้ทดสอบกับฮาร์ดแวร์จริง

## เครื่องพิมพ์สลิปความร้อน 58 มม. (Host 109.0.0)

เพิ่มความสามารถต่อเครื่องพิมพ์ความร้อนหน้ากว้าง 58 มม. เข้ากับเครื่องแม่ข่าย
พิมพ์สลิปให้นิสิตทุกครั้งที่ใช้บริการ และสั่งพิมพ์จากหน้าเว็บได้

**ไฟล์ใหม่ `ThermalPrinter.h`** — ไดรเวอร์ ESC/POS อิสระ ไม่รู้จักข้อมูลของระบบ
รับแค่สตริงไบต์ใส่คิวแล้วทยอยส่งออก จึง `#include` ไว้บนสุดของสเก็ตช์ได้
(ต่างจาก `WebPortal.h` ที่ต้องอยู่ท้ายไฟล์)

- สองช่องทางสลับด้วย `#define PRINTER_TRANSPORT_UART` บรรทัดเดียว
  - **0 = USB OTG host (ค่าเริ่มต้น)** — เขียนไดรเวอร์คลาสปริ้นเตอร์เองบน `usb_host_lib`
    เดินดูไบต์ของ config descriptor เอง เลือกอินเทอร์เฟซคลาส 0x07 (Printer) ก่อน
    แล้วค่อยถอยไป 0x0A (CDC Data) หาจุดปลาย bulk-OUT แล้วส่งทีละก้อน 64 ไบต์
  - **1 = UART TTL** — GPIO 17 (TX) / GPIO 18 (RX) / ร่วม GND ที่ 9600 8N1
- คิวพักได้ 8 ใบ คิวเต็มแล้วทิ้งใบใหม่ **ไม่บล็อกการตัดสิทธิ์** ไม่ว่ากรณีใด
- ใบที่ค้างคิวเกิน 2 นาทีโดยไม่พบเครื่องพิมพ์จะถูกทิ้งเอง กันอาการพ่นสลิปเก่า
  ที่หมดความหมายแล้วออกมารวดเดียวตอนเสียบเครื่องพิมพ์ทีหลัง
- มีสวิตช์ `ENABLE_THERMAL_PRINTER 0` ปิดทั้งระบบให้กลับไปเท่าเดิมทุกประการ

**เนื้อหาสลิป (อังกฤษ + ตัวเลขล้วน)** — หัวพิมพ์ราคาประหยัดไม่มีฟอนต์ไทยในตัว
ถ้าส่งไบต์ UTF-8 ไทยออกไปจะได้อักขระขยะเต็มม้วน

- `asciiSafe()` กรองให้เหลือ ASCII 32–126 · `asciiName()` บังคับเพิ่มว่าต้องมี
  ตัวอักษรอังกฤษอย่างน้อยหนึ่งตัว มิฉะนั้นใช้ข้อความสำรอง — เพราะชื่ออย่าง
  "ร้านที่ 1" ถ้ากรองเฉย ๆ จะเหลือแค่ "1" ซึ่งอ่านไม่รู้เรื่อง
- ชื่อนิสิตภาษาไทย → `STUDENT <รหัสนิสิต>` · ชื่อร้านภาษาไทย → เหลือหมายเลขร้าน
- ทุกบรรทัดถูกตรวจแล้วว่าไม่ล้น 32 ช่อง (ตัวใหญ่นับสองเท่า) เลขอ้างอิงยาว 28 ตัว
  จึงถูกย้ายไปขึ้นบรรทัดของตัวเอง ไม่ให้ถูกตัดขึ้นบรรทัดใหม่เอง
- สามแบบ: สลิปนิสิต · ใบสรุปยอดประจำวันพร้อมช่องลงนาม · สลิปทดสอบ

**จุดที่สั่งพิมพ์**

- พิมพ์อัตโนมัติทุกครั้งที่ตัดสิทธิ์สำเร็จ ทั้งจากการแตะบัตรและจากการตัดสิทธิ์ด้วยตนเอง
  เปิด/ปิดได้จากแท็บตั้งค่า จำค่าไว้ใน NVS คีย์ `prn_auto`
- **ข้ามรายการที่ซิงค์ย้อนหลัง** หลังลิงก์ขาด เพราะนิสิตรับอาหารและกลับไปแล้วตั้งแต่ตอนนั้น
  การพิมพ์ทีละหลายสิบใบจะล้นคิวและเปลืองกระดาษเปล่า ยอดยังอยู่ครบในใบสรุปและไฟล์ CSV
- ปุ่ม 🖨️ ต่อแถวในตารางรายชื่อ (เฉพาะรายการที่ตัดสิทธิ์แล้ว) = พิมพ์ซ้ำ
- ปุ่มพิมพ์ใบสรุปประจำวันบนแดชบอร์ด

**หน้าเว็บ** — การ์ดตั้งค่าเครื่องพิมพ์ในแท็บตั้งค่า (สวิตช์อัตโนมัติ + ปุ่มพิมพ์ทดสอบ +
สถานะการเชื่อมต่อและช่องทางที่คอมไพล์ไว้) และป้ายสถานะสดบนแดชบอร์ด
อัปเดตทุก 3 วินาทีผ่านคีย์ `printer` ใน `/api/dashboard`

**ปลายทางใหม่** `/api/print/test` · `/api/print/slip` · `/api/print/daily` ·
`/api/settings/printer` ทั้งหมดเป็น POST และผ่าน `requireAuth()`

**แก้เลขเวอร์ชันที่ค้าง** — `APP_VERSION` ของแม่ข่ายยังเป็น `108.0.0` ทั้งที่
CHANGELOG ไประบุ 108.3.0 แล้ว และตาราง README ยังบอกสถานีเป็น 118.0.0
ทั้งที่โค้ดจริงเป็น 119.0.0 → แก้ให้ตรงกันทั้งหมด

> **ยังไม่ได้คอมไพล์และยังไม่ได้ทดสอบกับเครื่องพิมพ์จริง** — สภาพแวดล้อมที่พัฒนา
> เข้า `downloads.arduino.cc` ไม่ได้ (ถูกนโยบายพร็อกซีปิดกั้น) จึงติดตั้ง toolchain
> ไม่ได้ ส่วนที่เสี่ยงที่สุดคือไดรเวอร์ USB host เพราะ ESP-IDF ไม่มีไดรเวอร์คลาส
> ปริ้นเตอร์มาให้ ต้องเขียนเองทั้งหมด ถ้า USB ไม่ยอมทำงานให้ตั้ง
> `PRINTER_TRANSPORT_UART` เป็น 1 แล้วต่อสาย TTL แทน เนื้อหาสลิปและปุ่มบนหน้าเว็บ
> ทำงานเหมือนกันทั้งสองช่องทาง

## ความทนทานของลิงก์และคิวออฟไลน์ (Host 108.3.0 / Station 119.0.0)

ตรวจคุณสมบัติการเชื่อมต่อทั้งสองฝั่งสำหรับระยะห่างราว 20 เมตร

**การตั้งค่าวิทยุ**

- กำลังส่งเดิมตั้งไว้ `esp_wifi_set_max_tx_power(68)` = 17 dBm ทั้งที่ ESP32-S3 ตั้งได้ถึง
  80 = 20 dBm → ปรับขึ้นทั้งสองฝั่ง
- เพิ่ม `WIFI_PROTOCOL_LR` คู่กับ 11b/g/n ทั้งสองฝั่ง โทรศัพท์ยังต่อ Wi-Fi ได้ตามปกติ
- เพิ่มสวิตช์ `ESPNOW_FORCE_LONG_RANGE_RATE` สำหรับบังคับอัตราส่ง LoRa 250 kbps
  ตั้งค่าเริ่มต้นไว้ที่ปิด เพราะถ้าเปิดข้างเดียวทั้งสองเครื่องจะคุยกันไม่รู้เรื่อง

**การส่งซ้ำ**

- เดิมถ้า unicast ล้มเหลวจะยิงเป็น broadcast แทน ซึ่ง**ทำให้แย่ลง** เพราะ broadcast
  ไม่มี ACK ระดับ MAC จึงไม่มีการส่งซ้ำอัตโนมัติของชิป ขณะที่ unicast มี ARQ ในตัว
  → ใช้ broadcast เฉพาะตอนยังไม่รู้จัก MAC ของอีกฝั่ง
- สถานีลงทะเบียน `onDataSent` ไว้แต่ทิ้งสถานะ (`(void)status`) → นำมาใช้ยิงซ้ำทันที
  ที่ชิปแจ้งว่าส่งไม่ถึง แทนการรอครบช่วงเวลา
- แม่ข่ายไม่เคยลงทะเบียน send callback เลย → เพิ่ม และยิงคำตอบการสแกนซ้ำได้อีก 2 ครั้ง
- เดิมส่งซ้ำแค่ 3 ครั้ง ห่างกัน 450 ms ใช้เวลาไป 1.35 วินาทีจาก timeout 3 วินาที
  เหลือเวลาว่างเปล่า 1.65 วินาที → เปลี่ยนเป็น 7 ครั้งภายในเวลาเท่าเดิม ถี่ช่วงแรกแล้วห่างออก

**คิวออฟไลน์ฝั่งสถานี** — ช่อง `StationPacket.offlineTime[24]` มีอยู่ในโครงสร้างตั้งแต่แรก
แต่ไม่เคยถูกใช้ทั้งสองฝั่ง ตอนนี้นำมาใช้งานจริง

- แม่ข่ายไม่ตอบ → บันทึกการแตะลง NVS (48 รายการต่อสถานี) แล้วขึ้นจอบอกให้จ่ายอาหารได้เลย
  แทนการขึ้นจอแดงปฏิเสธนิสิตโดยไม่เหลือร่องรอย
- ลิงก์กลับมา → ส่งเข้าระบบเองทีละรายการ โดยใช้เวลาตอนแตะบัตรจริงเป็นเวลารับสิทธิ์
- แม่ข่ายข้ามการตรวจช่วงเวลาบริการให้รายการย้อนหลัง จึงไม่ถูกปฏิเสธเพราะซิงค์หลังปิดบริการ
- บัตรใบเดิมแตะซ้ำที่สถานีเดียวกันระหว่างออฟไลน์จะไม่ถูกบันทึกซ้ำ
- ถ้ารู้อยู่แล้วว่าแม่ข่ายหลุด ลดเวลารอต่อการแตะจาก 3 วินาทีเหลือ 1.5 วินาที
- ประเภทบัตรในบันทึกต่อท้ายว่า `(Offline)` ให้เจ้าหน้าที่แยกออกได้
- หน้าแรกของสถานีเปลี่ยนบรรทัดล่างเป็นคำเตือนสีเหลืองบอกจำนวนรายการที่รอส่ง

---

## เติมข้อมูลหน้า 2 ของเครื่องประจำร้านค้า (Station 118.2.0)

เดิมการ์ดสองใบจบที่ y=160 แต่แถบล่างเริ่มที่ y=218 เหลือพื้นที่ว่างเปล่า 58 พิกเซล
คิดเป็น 24% ของความสูงจอ และการ์ดขวาเป็นข้อความคงที่ทั้งใบ (QUOTA / LIMIT / RADIO / CH 1)
ซึ่งไม่มีวันเปลี่ยนค่า

**ที่เพิ่มเข้ามา**

- **รายการที่จ่ายล่าสุดสามรายการของร้านนี้** เต็มแถบล่างที่เคยว่าง แสดงเวลา
  รหัสนิสิตแบบปิดบังห้าหลักแรก และจำนวนเงิน เก็บเป็นคิววนในแรม
- **ภาพรวมทั้งโรงอาหาร** แทนที่การ์ดขวาที่เป็นข้อความคงที่ ด้วยยอดใช้สิทธิ์ทั้งระบบ
  เทียบผู้มีสิทธิ์ทั้งหมด แถบความคืบหน้า สัดส่วนเป็นเปอร์เซ็นต์ และสถานะเปิด-ปิดบริการ
  พร้อมช่วงเวลา — แม่ข่ายฝากมากับช่อง `message[32]` ที่ว่างอยู่ในแพ็กเก็ต heartbeat
  **ขนาดแพ็กเก็ตไม่เปลี่ยน** และเฟิร์มแวร์รุ่นเก่าที่ไม่อ่านช่องนี้ก็ยังทำงานได้ตามปกติ
- **จำนวนครั้งที่ไม่สำเร็จและเวลาที่จ่ายล่าสุด** ในการ์ดซ้าย ซึ่งเดิม `totalScansToday`
  ถูกนับไว้แต่ไม่เคยถูกนำมาแสดงที่หน้าไหนเลย
- ถอดป้าย ONLINE ออกจากการ์ดซ้าย เพราะซ้ำกับตัวบอกสถานะบนแถบบนอยู่แล้ว
  และเดิมวางห่างข้อความ TODAY SERVED แค่ 2 พิกเซลจนดูเหมือนชนกัน
- แยก `displayStatsDashboard()` เป็นส่วนคงที่กับส่วนที่เปลี่ยนค่า จึงรีเฟรชทุก heartbeat
  ได้โดยจอไม่กะพริบ
- แก้เอกสาร: ตารางโปรโตคอลใน README ยังค้างขนาด `HostConfigPacket` ไว้ที่ 5 ไบต์
  จากตอนที่ยังไม่ได้ขยายเป็น 8 ไบต์

---

## ปรับหน้าแรกของเครื่องประจำร้านค้า

- เพิ่มสัญลักษณ์บัตร RFID กลางหน้าจอ เป็นรูปบัตรพร้อมชิปและคลื่น contactless สามชั้น
  วาดด้วย `drawRoundRect`, `fillRoundRect` และ `drawCircleHelper` ของ Adafruit GFX ล้วน ๆ
  จึงไม่กินพื้นที่แฟลชเพิ่มเหมือนการฝังบิตแมป และเปลี่ยนสีตามธีมได้
- ยกข้อความสิทธิ์ต่อวันออกมาเป็นการ์ดของตัวเองที่มีขอบสีเหลือง จากเดิมเป็นบรรทัดเล็ก
  ขนาด 1 อยู่ท้ายการ์ดเดียวกับข้อความอื่น ตอนนี้ **35 THB / DAY** ใช้ตัวอักษรขนาด 3
  จัดกึ่งกลาง อ่านได้จากระยะไกลของเคาน์เตอร์ โดยมีคำว่า SUBSIDY QUOTA กำกับด้านบน
- ข้อความเดิมอยู่ครบทุกบรรทัด: `READY FOR RFID CARD TAP`, `TAP CARD HERE`
  และ `Protocol: ESP-NOW Channel 1 Secured` (ย้ายมาอยู่นอกการ์ด จัดกึ่งกลางจอ)

---

## แก้ปัญหาพอร์ทัลใช้งานไม่ได้ในหน้าต่าง Captive Portal

ตรวจสอบพฤติกรรมตอนโทรศัพท์หรือคอมพิวเตอร์เชื่อมต่อ Wi-Fi ของเครื่องแม่ข่าย

**บั๊กที่พบและแก้แล้ว** — หน้าพอร์ทัลพังทั้งหน้าในเบราว์เซอร์ที่ `localStorage` ใช้ไม่ได้
ซึ่งเป็นกรณีของหน้าต่าง Captive Portal บน iOS/Android และโหมดไม่ระบุตัวตนพอดี
`initTheme()` เรียก `localStorage.getItem()` โดยไม่หุ้ม try/catch เมื่อเบราว์เซอร์โยน
SecurityError สคริปต์จึงตายตั้งแต่บรรทัดแรกของการเริ่มต้น ผลคือ `bindAjaxForms()`,
`bindDelegatedActions()` และ `refreshDashboard()` ไม่เคยถูกเรียก ฟอร์มทุกใบไม่ทำงาน
ปุ่มในตารางกดไม่ได้ และแดชบอร์ดค้างอยู่ที่แถบ skeleton ตลอดไป
→ เพิ่มตัวช่วย `lsGet()` / `lsSet()` ที่หุ้ม try/catch และแยกการเรียกขั้นเริ่มต้นออกจากกัน
เพื่อไม่ให้ขั้นใดขั้นหนึ่งล้มแล้วลากส่วนที่เหลือตายไปด้วย

**ส่วนที่ทำงานถูกต้องอยู่แล้ว** — การดัก DNS และการตอบ 302 ทำให้ Android, iOS,
Windows และ Firefox เด้งหน้าต่างเข้าสู่ระบบขึ้นมาเองครบทุกระบบ

**ที่ปรับเพิ่มเพื่อความทนทาน**

- ส่ง `Location` เป็น URL แบบเต็ม (`http://192.168.4.1/login`) แทนเส้นทางสัมพัทธ์
  เพราะตัวตรวจจับของบางระบบปฏิบัติการดูโฮสต์ในส่วนหัวนี้ และแนบหน้า HTML
  ที่มี meta refresh ไปด้วยสำหรับเครื่องที่ไม่ตามส่วนหัว Location
- ตั้ง `dnsServer.setTTL(0)` และ `setErrorReplyCode(NoError)` เพื่อไม่ให้โทรศัพท์
  จำการชี้โดเมนทั้งหมดมาที่ `192.168.4.1` ไว้หลังตัดการเชื่อมต่อไปแล้ว
- เพิ่ม `Cache-Control: no-store` ในผลตอบกลับของ captive portal
- ตอบ 204 ให้ `/favicon.ico` แทนที่จะปล่อยให้ตกไปที่ onNotFound แล้วโหลดหน้า
  ล็อกอินทั้งหน้ามาเป็นไอคอน
- เพิ่มลิงก์ไปหน้าสถานะโรงอาหารบนหน้าล็อกอิน สำหรับคนที่ไม่ใช่เจ้าหน้าที่
  ซึ่งเดิมเจอแต่ช่องกรอกรหัสที่ผ่านไม่ได้

---

## ซิงค์โหมดการแสดงผลจากแม่ข่ายไปสถานี (Host 108.2.0 / Station 118.1.0)

เครื่องแม่ข่ายเป็นเจ้าของโหมดการแสดงผลของทั้งระบบ

- ขยาย `HostConfigPacket` จาก 5 เป็น 8 ไบต์ เพิ่ม `screenOn`, `screensaver` และ `modeSeq`
  (`static_assert` ยืนยันขนาดทั้งสองฝั่ง เฟิร์มแวร์คนละรุ่นจะไม่ซิงค์กันแต่การแตะบัตรยังทำงานปกติ)
- **ธีมสว่าง/มืด บังคับเสมอ** — แม่ข่ายย้ำคำสั่งทุกครั้งที่ตอบ heartbeat สถานีที่ค่าไม่ตรงจะเปลี่ยนตาม
  ภายในไม่กี่วินาที การกดสามครั้งที่สถานีจึงขึ้นหน้าแจ้งว่าธีมถูกกำหนดจากแม่ข่ายแทนการสลับเอง
- **โหมดพักหน้าจอ** — แม่ข่ายเข้า/ออกโหมดพักหน้าจอ ทั้งจากการกดสองครั้งและการหมดเวลา 5 นาที
  จะสั่งให้ทุกสถานีทำตาม และการแตะบัตรที่จุดใดก็ตามจะปลุกทั้งระบบพร้อมกัน
- **ไฟหน้าจอ** — เพิ่มการกดค้าง 1.5 วินาทีที่แม่ข่ายเพื่อปิด/เปิดไฟหน้าจอ ซึ่งเดิมมีแต่ฝั่งสถานี
  และสั่งให้ทุกสถานีดับจอตาม เมื่อจอแม่ข่ายดับ ระบบจะหยุดวาดจอเพื่อลดงานที่ไม่จำเป็น
- โหมดพักหน้าจอและไฟหน้าจอเป็นคำสั่งแบบ "สั่งเมื่อเปลี่ยน" ดูจาก `modeSeq` ที่เพิ่มขึ้น
  สถานีจึงยังกดปิดจอของตัวเองได้โดยไม่ถูกสั่งเปิดกลับทุกรอบ heartbeat
- ถ้าสถานีกำลังแสดงผลการแตะบัตรอยู่ จะเลื่อนการเปลี่ยนโหมดไปทำหลังจอผลลัพธ์หมดเวลา
  ไม่ตัดจอกลางคันระหว่างที่นิสิตกำลังอ่านผล
- คำสั่งเปลี่ยนโหมดถูก broadcast สามครั้งห่างกัน 8 มิลลิวินาที เพราะ ESP-NOW แบบ broadcast
  ไม่มีการยืนยันการรับ

---

## เพิ่มจอสาธารณะ (Host 108.1.0)

เพิ่มหน้า `/display` สำหรับต่อออกมอนิเตอร์จอใหญ่ในโรงอาหาร ให้นิสิตและร้านค้าเห็น
ยอดการใช้บริการแบบเรียลไทม์

- รีเฟรชตัวเองทุก 2 วินาทีจาก `/api/display` แสดงยอดใช้สิทธิ์ ยอดจัดสรร
  รายการที่ตัดสิทธิ์ล่าสุด 12 รายการ และสถานะร้านค้าทั้งสี่
- **เปิดดูได้โดยไม่ต้องเข้าสู่ระบบ** จึงไม่กินช่องผู้ใช้งานของเจ้าหน้าที่ที่มีอยู่ 3 ช่อง
- ข้อมูลที่ส่งออกถูกตัดให้เหลือเฉพาะที่เปิดเผยได้: ไม่มีชื่อนิสิต ไม่มีเลขบัตร
  ไม่มีเลขที่อ้างอิง ไม่มีข้อมูลฮาร์ดแวร์แม่ข่าย และรหัสนิสิตปิดบังเหลือห้าหลักแรก
- แสดงเฉพาะรายการที่ตัดสิทธิ์สำเร็จ ไม่แสดงบัตรซ้ำหรือบัตรที่ไม่อยู่ในทะเบียน
  เพื่อไม่ให้นิสิตถูกประจานบนจอสาธารณะ
- ไม่มีเมนูและไม่มีปุ่มสั่งงาน เปิดไปหน้าอื่นของระบบไม่ได้
- เพิ่มสวิตช์เปิด/ปิดหน้าจอสาธารณะในแท็บ จัดการระบบ → กำหนดเวลา & ระบบ
  (เก็บค่าใน Preferences คีย์ `pub_disp` ค่าเริ่มต้นคือเปิด)
- ขยายจำนวนเครื่องที่ต่อ Wi-Fi พร้อมกันได้จาก 4 เป็น 8 เพราะจอสาธารณะกินไปหนึ่งช่อง
- ใช้ฟอนต์ของระบบล้วน ๆ ไม่พึ่งเว็บฟอนต์ เพราะเครือข่ายของแม่ข่ายไม่มีทางออกอินเทอร์เน็ต
- ถ้าดึงข้อมูลไม่สำเร็จสองครั้งติดกันจะขึ้นแถบเตือนท้ายจอ แล้วพยายามเชื่อมต่อใหม่ต่อไปเอง
- คิวรายการถูกล้างเมื่อปิดยอดประจำวัน

---

## Host Server 108.0.0 / Station Client 118.0.0

ตรวจสอบโค้ดทั้งสองฝั่งจากรุ่น 107.0.1 / 117.0.7 แล้วแก้ข้อผิดพลาดที่พบ พร้อมปรับ UX/UI
ตามขอบเขตที่เลือกไว้ (จอ: ย่อฟอนต์อัตโนมัติ + สถานะสดจริง · เว็บ: toast/AJAX + แดชบอร์ดสด +
ตารางลื่นขึ้น · ความปลอดภัย: เต็มชุด · SPI: ย้ายจอไป HSPI)

---

### Station Client — ข้อผิดพลาดที่แก้

1. **บัส SPI ชนกัน** — `SPIClass SPI_TFT(FSPI)` อยู่บน peripheral เดียวกับตัวแปร `SPI`
   มาตรฐานของ ESP32-S3 ที่ไลบรารี MFRC522 ใช้ ทั้งสองอุปกรณ์จึงถูกตั้งขาคนละชุดบนบัสเดียวกัน
   → ย้ายจอไป `HSPI`
2. **คำสั่งสลับธีมจากแม่ข่ายถูกทิ้งทั้งหมด** — ตัวรับ ESP-NOW ตัดทุกแพ็กเก็ตที่
   `len != sizeof(HostResponsePacket)` (200 ไบต์) ส่วน `HostConfigPacket` มีแค่ 5 ไบต์
   → เพิ่มการรับ `MSG_CONFIG` และนำไปใช้จริงใน `applyPendingTheme()`
3. **แถบสถานะลิงก์ไม่เคยแสดง** — `updateTopRightHeaderSmooth()` ถูกเขียนไว้ครบแต่ไม่เคยถูกเรียก
   → เปลี่ยนเป็น `updateStationHeaderStatus()` เรียกจาก `drawStationTopBar()` และรีเฟรชทุก 1 วินาที
4. **หน้า Diagnostics ค้างนิ่ง** — `STATE_STATUS` ไม่เคยถูกกำหนดค่า เงื่อนไขรีเฟรชในลูปจึงไม่เคยเป็นจริง
   → เพิ่ม `showStationPage()` ที่ตั้ง state ให้ตรงกับหน้าที่แสดงอยู่เสมอ
5. **ค่าที่หายไปบนหน้า Diagnostics** — มีหัวข้อ `BATTERY VOLTAGE` และ `TODAY SERVED`
   แต่ไม่เคยพิมพ์ค่าลงไป → เติมค่าพร้อมสีบอกสถานะแบตเตอรี่
6. **จอผลลัพธ์ขึ้นช้าเกือบ 1 วินาที** — `soundError()` / `soundAlarm()` บล็อก 660–750 ms
   ก่อนคำสั่งวาดจอ → ย้ายไปเล่นหลังวาดเสร็จ
7. **ตัวนับซ่อนอยู่ในฟังก์ชันวาดจอ** — `totalSuccessToday++` อยู่ใน `displayResult()`
   → ย้ายไปนับที่ลูปหลักตอนได้รับคำตอบ `SUCCESS`
8. **ข้อความแบนเนอร์ล้นจอ** — `"! DUPLICATE: ALREADY CLAIMED !"` ที่ size 2 กว้าง 360px เกินจอ 320px
   และถูกตัดทิ้ง → เพิ่ม `drawFitCenteredText()` ที่ย่อขนาดฟอนต์อัตโนมัติ
9. **ADC บล็อกลูป** — `readBatteryVoltage()` ใช้ `delay(2)` แปดรอบ → เปลี่ยนเป็น
   `delayMicroseconds(300)` พร้อมแคชผล 3 วินาที
10. **เขียน NVS ถี่เกินจำเป็น** — บันทึกยอดทุก heartbeat (~ทุก 6 วินาที) → เขียนเฉพาะตอนค่าเปลี่ยนจริง
11. **`noInterrupts()` ไม่ได้ป้องกันอะไร** — callback ของ ESP-NOW ทำงานใน task ไม่ใช่ ISR
    → เปลี่ยนเป็น `portMUX` critical section
12. รูปแบบ `%d` กับตัวแปร `uint32_t` → แก้เป็น `%u` พร้อม cast

---

### Host Server — ข้อผิดพลาดที่แก้

1. **เลขอ้างอิงบนจอไม่ตรงกับที่บันทึกจริง** — หน้า 3 เรียก `generateRefNo()` ทุกครั้งที่วาดจอ
   ทำให้สร้างเลขใหม่เรื่อย ๆ และดัน `transactionCounter` ขึ้นโดยไม่มีธุรกรรมจริง
   → เก็บ `lastScannedRefNo` ไว้ตอนประมวลผลแล้วนำมาแสดง
2. **ชื่อที่มีลูกน้ำทำข้อมูลเพี้ยน** — `students.csv` และ `daily_log.csv` เขียนแบบไม่ครอบเครื่องหมายคำพูด
   → เพิ่ม `csvQuote()` / `parseCsvLine()` ใช้ทั้งการเขียน อ่าน นำเข้า และส่งออก
   (ยังอ่านไฟล์รุ่นเก่าที่ไม่มีเครื่องหมายคำพูดได้)
3. **ดาวน์โหลดไฟล์นอกคลังประวัติได้** — `/api/archive/download` ไม่กรองชื่อไฟล์
   จึงโหลด `/admins.json` ที่เก็บรหัสผ่านได้ → เพิ่ม `isSafeArchiveName()` บังคับ `arc_*.csv`
4. **Session token เดาได้** — `random()` ไม่เคยถูก seed → เปลี่ยนไปใช้ `esp_random()` 128 บิต
5. **Session หมดอายุผิดพลาดที่ 49 วัน** — เทียบ `millis() < expiry` ตรง ๆ → เปลี่ยนเป็นการเทียบแบบ wrap-safe
6. **คุกกี้ไม่มี SameSite** → เพิ่ม `SameSite=Strict; Max-Age=7200` และล้าง session ฝั่งเซิร์ฟเวอร์ตอน logout
7. **คำสั่งทำลายข้อมูลเป็น GET** — `/reset`, `/api/student/delete`, `/manual-claim`, `/bind-temp`,
   `/api/tempcard/remove`, `/api/archive/delete` → ย้ายเป็น POST ทั้งหมด
8. **ชื่อนิสิตที่มี `'` `"` `<` ทำตารางพัง (XSS)** — ตารางถูกประกอบด้วย `innerHTML` และ `onclick`
   ที่ฝังค่าลงในสตริง JavaScript → สร้างแถวด้วย DOM/`textContent` และใช้ `data-attribute`
   สำหรับปุ่มที่เรนเดอร์จากฝั่งเซิร์ฟเวอร์ พร้อม `htmlEscape()` ทุกจุดที่ยังต้องต่อสตริง HTML
9. **`limit` ไม่มีเพดาน** — `/api/students?limit=1000` ทำให้ `DynamicJsonDocument(8192)` ล้น
   จน JSON ที่ส่งกลับไม่สมบูรณ์ → จำกัดที่ 100 และปรับขนาดเอกสารตามจำนวนที่ขอจริง
10. **ไม่มีการจำกัดการเดารหัสผ่าน** → พิมพ์ผิด 5 ครั้งติดกันล็อก 1 นาที
11. **`/api/system/health` เปิดให้เข้าถึงโดยไม่ต้องเข้าสู่ระบบ** → บังคับยืนยันตัวตน
12. **`readHostBatteryVoltage()` บล็อก 16 ms ทุกครั้งที่วาดแถบบน** → แคช 3 วินาที และเลิกใช้ `delay()`
13. **แบนเนอร์ผลสแกนล้นจอ** เหมือนฝั่งสถานี → ย่อฟอนต์อัตโนมัติ
14. **`File root` ไม่ถูกปิด** หลังไล่รายการไฟล์ประวัติ → ปิดแล้ว
15. **ไม่มีการตรวจข้อมูลขาเข้า** — เพิ่มการตรวจรหัสนิสิตซ้ำ, เลขบัตรซ้ำ, ความยาวฟิลด์,
    ความถูกต้องของวันที่/เวลา และช่วงเวลาเปิด-ปิดบริการ
16. **แก้ไข UID ของนิสิตที่ถือบัตรสำรองอยู่แล้วไปทับบัตรชั่วคราว** → เขียนลง `originalUid` แทน
17. **ธีมถูกส่งซ้ำทุก heartbeat** — เมื่อฝั่งสถานีรับ `MSG_CONFIG` ได้แล้ว การส่งซ้ำจะไปทับ
    ธีมที่ผู้ใช้กดเองที่หน้าเครื่องสถานี → ส่งเฉพาะตอนสถานีเพิ่งออนไลน์หรือเจ้าหน้าที่สั่งเอง
18. รูปแบบ `%d` กับ `size_t` / `uint32_t` หลายจุด → แก้เป็น `%u` พร้อม cast

---

### เว็บพอร์ทัล — สิ่งที่ปรับปรุง

- **เลิกใช้ `alert()`** ที่บังคับรีโหลดทั้งหน้าและทำให้เสียแท็บที่ค้างอยู่ → ทุกคำสั่งส่งแบบ AJAX
  และแจ้งผลด้วย toast มุมขวาล่าง พร้อมสถานะ "กำลังบันทึก..." บนปุ่ม
- **แดชบอร์ดสด** — ดึง `/api/dashboard` ทุก 3 วินาที อัปเดตเกจโควตา ยอดจัดสรร อุณหภูมิ CPU
  แบตเตอรี่ นาฬิกา ป้ายสถานะเปิด/ปิดบริการ กราฟรายร้าน และการ์ดจุดบริการทั้ง 4 จุด
  (ออนไลน์/ออฟไลน์, แท่งสัญญาณ, RSSI, แบตเตอรี่, เวลาที่ได้ยินล่าสุด)
- **ตารางรายชื่อ** — หัวตารางตรึงขณะเลื่อน, skeleton ระหว่างโหลด, แถบไฮไลต์เมื่อชี้,
  ตัวเลขหน้าแสดงเป็น "หน้า/ทั้งหมด", ปุ่มมี tooltip อธิบายทุกปุ่ม
- **เลิกใช้ `prompt()`** สำหรับผูกบัตรสำรองและตัดสิทธิ์ด้วยตนเอง → เปลี่ยนเป็น modal
  ที่มี dropdown เลือกจุดบริการ ปิดได้ด้วยปุ่ม Esc หรือคลิกพื้นหลัง
- จำแท็บที่เปิดค้างไว้ และกลับมาที่แท็บเดิมหลังบันทึกข้อมูล
